# 2026-07-22

## Канонический многоязычный source-граф

- Backend разделён на общий английский source-граф и языковые переводы: добавлены `book_translations` и `segment_translations`, а повторный импорт нового языка больше не удаляет `paragraphs`, `segments` и `source_words`.
- Для новых книг source-ID стали детерминированными; существующие книги сохраняют свои канонические ID при неизменном source hash.
- Чтение Reader и сборка mobile package получили явный `target_lang`; RU/UK берут разные target-сегменты при одинаковых `paragraph_id`, `segment_id` и `word_id`.
- API chunked package и Workbench передают язык явно. Повторное использование пакета последнего импортированного языка исключено.
- В Workbench добавлен блокирующий lexicon-контракт: неизвестные source-ID, чужой segment ID, неверный target span и несовпадение contextual translation с фактическим target-span останавливают сборку ZIP.

## Миграция главы 1

- Пять проверенных книг главы 1 мигрированы на единый source-граф: 752 слова и 145 сегментов RU/UK сопоставлены без неоднозначностей.
- В зависимости от текущего канонического графа перенесены UK occurrence-ID у `A Voice from Online` и `The Wrong Classroom`, RU occurrence-ID — у `First Day`, `Two People, One City` и `Welcome to Maple Street`.
- Добавлен preview/apply-инструмент миграции с резервным копированием БД и изменяемых словарных файлов.
- Для 55 UK occurrences восстановлены точные контекстные украинские формы из утверждённых EN/UK сегментов; отдельно подтверждено `students → учнів` в `The Wrong Classroom`.
- Все пять UK-книг повторно прошли book-layer и occurrence validators со статусом `valid`, `unresolved=0`.

## Пакеты и проверки

- Пересобраны пять RU/UK ZIP и обновлены их хеши в CloudLibrary index.
- Во всех пяти ZIP наборы RU/UK source-ID полностью совпадают; каждый `reader_lexicon_<lang>` имеет 100% существующих `word_id`.
- Backend: 32/32 tests.
- Flutter: `flutter analyze` изменённых API/Workbench файлов — без замечаний; 27 mobile dictionary/library index tests — успешно.
- Прямой `dart run tool/rebuild_virgil_library_index.dart` не применён, потому что tool импортирует Flutter `dart:ui`; индекс обновлён детерминированным Python-инструментом по фактическим SHA-256 и размерам ZIP.

## Синхронизация R2

- Обновлённые ZIP пяти книг и `library_index.json` опубликованы в `r2books:books/virgil/library`; индекс загружен последним.
- Побайтовая проверка `rclone check --download` завершилась без различий: 141 файл совпадает, 0 различий.

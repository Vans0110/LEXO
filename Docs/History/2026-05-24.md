# 2026-05-24

## Сессия: тест американских Kokoro voices на первом абзаце книги

- Добавлен мини-скрипт `scripts/test_kokoro_american_voices.py`.
- В качестве тестового текста взят первый содержательный абзац из `Books/New Student.txt`.
- Сгенерированы wav для американских голосов Kokoro: `af_bella`, `af_sarah`, `am_adam`, `am_michael`.
- Результаты сохранены в `data/tts/kokoro_voice_tests/new_student_first_paragraph/`.
- Создан `manifest.json` с исходным текстом, speed `0.89`, путями к wav и длительностями.

## Сессия: обновление доступных Kokoro voices для LEXO/Nove

- В `engine/tts/tts_provider.py` оставлены старые `af_heart` и `am_michael`.
- Из доступных профилей убраны `af_nicole` и `bm_george`.
- Добавлены новые американские voices: `af_bella`, `af_sarah`, `am_adam`.
- Обновлены mobile download/settings списки в Nove под итоговый набор: `af_heart`, `af_bella`, `af_sarah`, `am_adam`, `am_michael`.
- Fast-fix: в `MobileLibraryScreen` добавлен отсутствующий `onPreferredVoiceChanged` для `MobileSettingsScreen`.
- Проверки: Python `py_compile` успешно; `dart analyze production_mobile` больше не показывает errors, остались существующие warnings/infos.

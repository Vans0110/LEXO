# 2026-06-02

## Workbench: статусы обработанных книг

- Добавлен отдельный загрузчик статусов книг верстака `nove_workbench_book_status.dart`.
- Статус теперь собирается из `workbench/output` и установленных zip в `assets/library`.
- В библиотеке верстака обработанные книги подсвечиваются отдельной рамкой/фоном.
- Плитка книги вынесена в `nove_workbench_book_tile.dart` и показывает справа обложку.
- Добавлены отдельные индикаторы: `TXT`, `RU TXT`, `UK TXT`, `Audio`, `Dict RU`, `Dict UK`.
- Проверка изменённых файлов: `dart analyze` без замечаний.

## Workbench: автостарт backend

- В `production_mobile/workbench/run_nove_workbench.bat` добавлен запуск backend через корневой `run_lexo_engine_lan.bat`.
- Перед запуском проверяется порт `127.0.0.1:8765`, чтобы не стартовать второй backend, если он уже работает.
- Верстак ждёт готовность backend до 20 секунд и продолжает запуск Flutter, если backend ещё догружается.

## Workbench: диагностика аудио-голосов

- Статус книги в Workbench теперь различает наличие mp3 и готовность плеера.
- Для каждой книги читаются `tts_manifest.jobs`, `tts_manifest.profiles`, `word_audio_manifest.voice_id`, количество segment mp3 и word mp3.
- Плитка показывает `Audio`, `Player`, `Word audio`, `Audio voices`, `Profiles`, `Word voice`.
- Если segment audio есть, но profiles не совпадают с audio jobs, `Player` подсвечивается как broken.
- Проверка изменённых Workbench-файлов: `dart analyze` без замечаний.

## Workbench: разделение player voices

- В диагностике книги добавлена карта готовых player voices по `tts_manifest.jobs` с уровнями (`Slow`/`Normal`/`Fast` или audio variant).
- Плитка Workbench теперь показывает `Player voices: voice (levels)` и отдельную строку `Missing player` для профилей из `tts_manifest.profiles`, у которых нет ready jobs.
- Статус `audio ready` уточнён до `audio partial`, если segment audio есть, но не все объявленные profiles имеют player jobs.
- Проверка изменённых Workbench-файлов: `dart analyze lib/src/workbench/nove_workbench_book_status.dart lib/src/workbench/nove_workbench_book_tile.dart` без замечаний.

## Workbench: карточки по разделам

- Обложка книги в плитке Workbench перенесена влево.
- Старые горизонтальные бейджи заменены на три секции: `TXT`, `Dictionary`, `Voice`.
- В каждой секции элементы показываются строками с галочкой/крестиком и конкретным именем: reader/dictionary language, player voice levels, missing player voices, word audio voice.
- Проверка изменённых Workbench-файлов: `dart analyze lib/src/workbench/nove_workbench_book_tile.dart lib/src/workbench/nove_workbench_book_status.dart` без замечаний.

## Workbench: refresh одной книги

- В плитку книги добавлена кнопка refresh рядом с `Use`.
- Refresh запускает проверку и дозаполнение конкретной книги через существующий Workbench pipeline: выбранные языки, словари, выбранные Kokoro voices и missing player voices из profiles, если такие voices доступны в backend.
- Критерий аудио исправлен: наличие любого segment audio больше не пропускает генерацию, если нет audio jobs для выбранных voice ids.
- Обложка в плитке увеличена до высокого превью слева.
- Проверка изменённых Workbench-файлов: `dart analyze lib/src/workbench/nove_workbench_book_library.dart lib/src/workbench/nove_workbench_book_tile.dart lib/src/workbench/nove_workbench_book_status.dart` без замечаний.

## Library data: удаление mock TTS profile

- Из `tts_manifest.json` удалён устаревший профиль `mock_en_default`/`mock_tts` для `New Apartment` и `Grandma's Dinner`.
- Пересобраны соответствующие zip-архивы в `production_mobile/assets/library/a1/chapters/books_zip`.
- Проверка: `rg` по workbench output/library и чтение `tts_manifest.json` внутри всех zip-архивов не нашли `mock_en_default`, `mock_tts` или `Mock English`.

## Backend/Workbench: стабильный book_id

- Backend import text теперь принимает опциональный `stable_book_key`; если он передан, `book_id` считается от стабильного ключа, а не от `target_lang + text`.
- Workbench передаёт ключ `nove|level|section|chapter_id|title`, поэтому RU/UK импорт одной книги получает один и тот же backend `book_id`.
- Старое поведение сохранено для импортов без `stable_book_key`.
- Проверка: `py_compile` для `engine/api.py` и `engine/storage.py`; `dart analyze` для Workbench/API показал только старые `deprecated_member_use` по `DropdownButtonFormField.value`.

## Library data: удаление zip-архивов

- Удалены все zip-архивы книг из `production_mobile/assets/library/**/books_zip`.
- `.gitkeep` в папках `books_zip` сохранены.
- Проверка: после удаления найдено `remaining=0` zip-архивов в `books_zip`.

## Workbench data: удаление output-пакетов

- Удалены рабочие output-папки книг `production_mobile/workbench/output/**/book_*`.
- Проверка: после удаления найдено `remaining=0` папок `book_*` в `workbench/output`.

## Backend: RU context dictionary resolver

- Добавлен `engine/context_dictionary_resolver.py`: RU dictionary entry собирается из Wiktionary primary + FreeDict secondary без 2Books и без ручных override-слов.
- Resolver принимает `surface/lemma/POS`, `source_segment` и `target_segment`, чистит wiki-разметку/ударения, ранжирует переводы по совпадению с RU target segment и отдаёт одну combined entry.
- `engine/storage.py` переключает `dictionary_manifest` для `ru` на source `wiktionary_freedict_en_ru_context_v1`; `uk` оставлен на текущем `nllb_en_uk`.
- Workbench теперь считает RU dictionary свежим по новому source, UK — по `nllb_en_uk`.
- Проверка: `py_compile` для `engine/context_dictionary_resolver.py` и `engine/storage.py`; `dart analyze production_mobile/lib/src/workbench/nove_workbench_screen.dart` показал только старые deprecated `DropdownButtonFormField.value`.

## Workbench: refresh dictionaries по книге

- В плитку книги добавлена отдельная кнопка `Refresh dictionaries`.
- Кнопка обновляет только словари выбранных языков Workbench, не трогая TXT, cover, audio, player profiles и word audio.
- `NoveWorkbenchBuilder.refreshDictionaries` перезаписывает `dictionary_<lang>.json`, обновляет `load_manifest.json`, пересобирает zip и перестраивает `library_index.json`.
- Fix: refresh dictionaries всегда скачивает свежий backend package для выбранного языка, чтобы не записывать пустой cached `dictionary_manifest` после text-only import.
- Проверка: `dart analyze` для изменённых Workbench-файлов показал только старые deprecated `DropdownButtonFormField.value`.

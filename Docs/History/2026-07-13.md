# 2026-07-13

## Reorder-aware mobile grammar blocks

- Mobile POS grouping теперь завершает конструкцию `AUX + DET + ADJ` на прилагательном: `is a new` → `новая`, не захватывая `student` → `ученица`.
- `ADP` разрешён как начало grammar-блока и присоединяется к следующему lexical head: `at Hill` → `Хилл`; `School` независимо сохраняет `школы`.
- Target spans остаются независимыми и могут идти в обратном порядке (`School` → индекс 3, `at Hill` → индекс 4), поэтому перестановка `at Hill School` → `школы Хилл` больше не требует монолитного блока.
- Добавлен regression-тест полного случая `Sara is a new student at Hill School`; точечный Flutter test проходит, Dart analyze ошибок не выявил.
- В RU book layer `The Wrong Classroom` добавлены контекстные phrase entries `is a new` → `новая`, `at Hill` → `Хилл`; book-local формы исправлены на `student` → `ученица`, `School` → `школы`.
- Debug APK установлена на `emulator-5554`; локальный package книги обновлён теми же alignment entries и приложение перезапущено.
## Contextual target forms in translation strip

- Dictionary translation больше не заменяет полный перевод сегмента при отсутствии точного target span; fallback верхней панели всегда сохраняет sentence context.
- Для одиночных слов добавлено контекстное сопоставление словоформ по устойчивому общему основанию: `английский` находит `английского`, аналогично падежным/родовым формам.
- Добавлен widget regression-test: `English` с dictionary value `английский` показывает `Её урок английского языка` и target span `английского`.
- На `emulator-5554` установлена новая debug APK; live UI-проверка после tap по `English` подтвердила полное предложение и отсутствие standalone `английский`.
## Library dictionary v2: contextual variants and provenance

- Формальный контракт вынесен в `Docs/LIBRARY_DICTIONARY_CONTRACT.md`: lemma+POS, несколько подтверждённых переводов, provenance по книге/словоформе, контекстный выбор по target segment и запрет `translations.first`.
- Book-layer generation сохраняет контекстные target-словоформы; существующие 10 RU и 10 UK слоёв обогащены из параллельных сегментов.
- Global words/phrases переведены на schema/source v2 с `variants[]`, `book_ids[]`, `source_forms[]`; повторная rebuild-проверка дала `changed=false` для RU и UK.
- Сохранены одновременно базовые и контекстные формы, например `english|ADJ`: `английский` / `английского`, `school|PROPN`: `школы` / `школа`, `room|NOUN`: `комната` / `комнату` / `комнате` / `комнат`.
- Workbench word-to-word выбирает перевод, реально присутствующий в target sentence, включая близкую словоформу, вместо первого элемента глобального списка.
- Пересобраны dictionary/word_to_word артефакты и ZIP для 10 книг в RU/UK; исправлено закрытие ZIP-only package перед атомарной заменой на Windows.
- Проверки: backend unittest — 3/3; mobile Flutter regression — 6/6; Dart analyze и Python py_compile — без ошибок.

## Occurrence-level contextual alignment

- Исправлена перезапись повторных слов через map `lemma+POS`: `word_to_word` теперь содержит отдельную запись для каждого `word_id`.
- Выбор перевода выполняется среди вариантов глобального словаря в target-тексте текущего сегмента с positional scoring; уже назначенные target-токены резервируются и не могут достаться другому source-word.
- Полные многословные соответствия имеют приоритет над укороченной словоформой, точное совпадение — над fuzzy.
- Для `Her English class is in Room fourteen.` добавлены подтверждённые книгой варианты: `English` → `английского языка`, `class` → `урок`, `Room` → `классе`, `fourteen` → `четырнадцатом`.
- Та же occurrence-level логика добавлена в стандартный Dart Workbench export; Python refresh и Dart export больше не используют независимый `translations.first`-подобный выбор.
- RU global dictionary и 10 RU/UK package ZIP пересобраны; повторный global check дал `changed=false`.
- В debug-эмуляторе обновлён установленный package `The Wrong Classroom`; Reader оставлен на проверяемом предложении после tap по `class`.
- Проверки: backend 4/4, mobile Flutter 6/6, Dart analyze и Python py_compile — без ошибок.

## Clean phrase and component-word cards

- Устранено смешивание трёх контуров: dictionary-backed mobile normalization очищает legacy `source_first_*`/`unit_translation_*` перед применением `word_to_word`.
- Phrase alignment теперь отвечает за общий заголовок блока, а секция `Words` берёт отдельные переводы каждого `word_id`; служебные слова внутри phrase сохраняют компонентный перевод.
- Global phrases поддерживает `components[]` с lemma/POS/translation и `book_ids`; штатный phrase seed builder больше не отбрасывает components.
- Для `walk into` сохранено: phrase `входит в`, components `walk` → `входит`, `into` → `в`; global word `walk|VERB` получил подтверждённую форму `входит`.
- Пустой пользовательский блок `Source-first` (Units 0 / Groups 0) скрыт.
- Пересобраны global dictionaries и 10 RU/UK ZIP; debug APK установлен, package книги обновлён.
- Live long-press проверка на эмуляторе: `walk into` → `входит в`; Words: `walk` → `входит`, `into` → `в`; Source-first отсутствует.
- Проверки: backend 3/3, Flutter 6/6, Python py_compile без ошибок; Dart analyze — только существующие deprecated-info `withOpacity`.

## Repository-backed Codex skill for two-pass phrase audit

- Создан постоянный источник skill `Skills/lexo-phrase-audit`; пользовательская копия устанавливается в `%USERPROFILE%\.codex\skills\lexo-phrase-audit`.
- Skill требует два независимых прохода по каждой паре `parallel[]`: первичное извлечение phrase/components и повторный omission audit всех сегментов.
- Добавлена формальная схема phrase type/components/occurrences и `phrase_audit.reviewed_segments`/`second_pass.unresolved_omissions`.
- Добавлен детерминированный `validate_phrase_layer.py`: проверяет source/target evidence, components, причины пустых переводов, покрытие всех parallel-сегментов и успешный второй проход.
- Валидатор прошёл корректный fixture и ожидаемо отклонил текущий legacy layer The Wrong Classroom: 33 ошибки, включая отсутствие audit metadata, type/occurrences и components у большинства фраз.
- Добавлен `Scripts/InstallCodexSkills.ps1`; временная установка проверена, затем skill установлен и штатно провалидирован в пользовательском Codex.
- Репозиторный источник переживает переустановку Codex/Windows при восстановлении проекта и повторном запуске установщика.

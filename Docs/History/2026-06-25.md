# 2026-06-25

## Workbench: scoped Clean selected
- `Clean selected` в Virgil Workbench переведён с полного удаления книги на очистку только отмеченных галочками частей: text RU/UK, dictionary RU/UK и выбранные Kokoro voices.
- Для локального output/ZIP удаляются только соответствующие `reader_*.json`, `word_to_word_*.json`, `dictionary_*.json`, TTS jobs/audio выбранных voice и word-audio выбранных voice; оставшиеся файлы и manifest пересобираются.
- Добавлен backend endpoint `/books/clean-artifacts` для удаления только TTS rows/files/package-state выбранных `voice_id` без удаления reader, словарей и backend-книги.
- Проверки: `dart format`, `dart analyze` по затронутым Dart-файлам; `py_compile` для `api.py` и `storage.py`.

# 2026-07-20

## Полнота исходного репозитория

- Проверено соответствие рабочего дерева и `origin/main`: удалённый репозиторий оставался на состоянии 2026-07-15 и не содержал актуальный Block-контур.
- Корневой README дополнен требованиями, картой исходников и фактической схемой запуска Workbench через общий Flutter-проект и локальный backend.
- `.gitignore` отделяет исходники от scratch-файлов, локального аудио и бинарных release-артефактов; пример конфигурации остаётся версионируемым.
- В публикацию включаются только исходный код, skills, тесты, схемы и история; книги, модели, кэши, credentials и сгенерированные пакеты остаются локальными.

## Global Blocks

- Удалён рабочий контракт `global_phrases_<lang>.json`; вместо него используется `global_blocks_<lang>.json`.
- Зафиксированы ровно три глобальных словаря на язык: `global_words`, `global_blocks`, `global_function_words`.
- RU Global Blocks мигрирован на обязательные `type` и `explanation`; `for the rest of the day` сокращён до переиспользуемого `the rest of`.
- Старые UK-фразы не перенесены автоматически: композиционные группы нельзя объявлять блоками без нового аудита. UK Global Blocks создан пустым до последовательной проверки книг.

## Skills

- `$lexo-phrase-audit` заменён на `$lexo-block-audit` и установлен в пользовательский каталог Codex.
- Новый аудит требует минимальный непрерывный блок, универсальное пояснение, независимые omission/necessity-проходы и occurrence-level доказательство.
- Обновлены `$lexo-build-ru-book-layer` и `$lexo-add-ru-books-to-globals`, их схемы, валидаторы и тесты.
- Добавление блоков в Globals теперь блокирует отсутствующие или конфликтующие `type`/`explanation`.

## Runtime и UI

- Backend, Workbench, `reader_lexicon`, package repository и mobile reader переведены с `phrases` на `blocks`/`block_alignments`.
- Метаданные блока (`type`, `translation`, `explanation`) проходят до мобильной карточки.
- В detail sheet добавлен самостоятельный белый раздел `Blocks`; `Words` остаётся отдельным разделом.
- Для `the rest of` блоком владеют только конкретные позиции `the/rest/of`; `for`, второе `the` и `day` остаются самостоятельными словами.
- Пересобраны 10 актуальных двухъязычных ZIP-пакетов.

## Проверки

- `validate_block_layer.py`: 24/24 сегмента, 5 блоков, 0 ошибок, 0 предупреждений.
- `validate_ru_book_layer.py`: 81 слово, 146 occurrences, 5 block occurrences, 0 ошибок.
- Backend unit tests: 6/6 успешно.
- Add-to-globals tests: 2/2 успешно; повторный preview книги — 105 `skipped_existing`, 0 изменений.
- Verification word-to-word tests: 4/4 успешно.
- Flutter `mobile_dictionary_interaction_test.dart`: 10/10 успешно.
- `flutter analyze` выполнен: новых compile errors нет; остаются 20 ранее существовавших lint/info предупреждений проекта.
- Все три изменённые skills прошли `quick_validate.py`.

## Исправление occurrence-подсветки блока

- Для `the rest of` исправлена граница владения переводом: блок владеет точным контекстным span `конца`, отдельные слова сохраняют `for → до` и `day → дня`.
- `$lexo-block-audit` теперь явно разделяет occurrence-перевод и учебное пояснение: `translation` обязан буквально присутствовать в каждом доказательном target-сегменте; словарная перефразировка допускается только в `explanation`.
- Валидатор блокирует книжный блок, если его occurrence-перевод отсутствует в указанном target-сегменте. Backend и Workbench больше не подставляют первый глобальный перевод при отсутствии контекстного совпадения.
- Исправлены seed/book/verification/global block-данные, обновлены runtime-артефакты и пересобраны 10 RU/UK ZIP.
- Проверки: оба книжных валидатора — 0 ошибок; backend unit tests — 4/4; Flutter tests — 27/27; обе копии `$lexo-block-audit` прошли `quick_validate.py`.
- В `LEXO_Pixel_6` заново загружен актуальный ZIP: при выборе `the rest of` подтверждена подсветка `конца` в русском сегменте.

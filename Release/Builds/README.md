# Virgil Releases

`BUILD_RELEASE.bat` creates one immutable folder per `pubspec.yaml`
version:

```text
0.1.0+1/
  Virgil-0.1.0+1.aab
  SHA256.txt
  certificate.txt
  release-notes.md
```

Upload the AAB from that version folder to Google Play.

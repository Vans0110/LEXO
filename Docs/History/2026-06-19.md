# 2026-06-19

## Заполнение словарных JSON A1 Chapter 9

- В пяти книгах `Weather & Nature` найдено 118 уникальных пустых
  `lemma|POS`: 169 вхождений RU и 169 UK.
- 32 точные статьи уже существовали в Core; создано 86 новых точных статей
  RU/UK без изменения 1005 существующих Core-статей и без title-`PROPN`
  перенаправлений.
- Обновлены только пустые записи десяти словарных JSON главы 9: всего 338
  вхождений; ранее заполненные manifest-записи сохранены без изменений.
- Итог главы 9: 5 книг, 10 JSON, 976 словарных вхождений, пустых — 0.
- Core содержит 1091 статью; импортёр идемпотентен, языкового смешения RU/UK
  и ошибок `entry_count` нет, шесть профильных backend-тестов проходят.
- Во время проверки появились 10 JSON главы 10 с 208 пустыми вхождениями;
  они не изменялись в рамках этого шага.
## Пересборка словарных JSON двух актуальных книг

- Для актуальных `book_d3a4ed95015a_new_student` и
  `book_90d0dfac4455_grandma_s_dinner` заново собраны `dictionary_ru.json`
  и `dictionary_uk.json` штатным backend-контуром.
- Исходный текст обеих книг в backend-базе побайтово совпадает с текущими TXT:
  `New Student` из главы 5 и `Grandma's Dinner` из главы 2.
- Свежие словари содержат соответственно по 91 и 83 заполненные статьи на
  язык; пустых переводов, ошибок `entry_count`, `word_found=false` и
  расхождений со свежим package manifest нет.
- Обложки, reader JSON, переводы текста, аудио и остальные файлы книг не
  изменились; это подтверждено сравнением SHA-256 до и после пересборки.
- Шесть профильных backend-тестов проходят.
## Удаление двух устаревших output-книг

- Удалены устаревшие output-каталоги `book_8f20d27cd2c5_new_student`
  (`New Student`, прежняя глава 1) и `book_dc0b0425c6ec_grandma_s_dinner`
  (`Grandma’s Dinner` из старого `production_mobile`).
- Их актуальные замены не изменялись: `book_d3a4ed95015a_new_student`
  находится в главе 5 и содержит по 91 заполненной статье RU/UK;
  `book_90d0dfac4455_grandma_s_dinner` находится в главе 2 и содержит по
  83 заполненной статье RU/UK.
- Старые book ID отсутствуют в `CloudLibrary`, runtime и mobile-данных;
  дублей названий и stale source-path среди оставшихся output-книг нет.
- Во время проверки обнаружены уже созданные output-словари пяти книг главы 9:
  общий набор вырос до 45 актуальных книг и 90 JSON. В новых JSON главы 9
  остаётся 338 пустых RU/UK-вхождений; они не изменялись в рамках этого шага.
## Заполнение пустых статей существующих словарных JSON

- Проверены только уже созданные `dictionary_ru.json` и `dictionary_uk.json`:
  найдено 257 уникальных пустых `lemma|POS`, 365 пустых вхождений на язык.
- 30 точных статей уже присутствовали в `Virgil Core Dictionary`; создано
  227 новых точных статей RU/UK без изменения 778 существующих Core-статей.
- Не применялись title-`PROPN` fallback или перенаправления: для каждого
  запрошенного manifest-ом ключа создана самостоятельная точная статья.
- Обновлены только пустые записи в 50 manifest-файлах: всего 730 вхождений;
  ранее заполненные записи сохранены без изменений.
- После обновления Core содержит 1005 статей; во всех 84 существующих
  словарных JSON пустых записей нет, `entry_count` согласован с содержимым.
- Добавлен идемпотентный инструмент
  `Studio/Tools/add_a1_chapters_4_8_dictionary_entries.py`; повторная проверка
  создаёт 0 статей. Шесть профильных backend-тестов проходят.
## A1 Chapter 10 — обложки книг

- Для главы `Numbers & Money` созданы пять финальных обложек:
  `The Lost Wallet`, `The Phone Fund`, `Three Prices`, `Market Day`,
  `The Cafe Bill`.
- Сцены построены по полным текстам, универсальному контракту A1 и
  утверждённым сюжетным брифам; эталон серии использован только как
  визуальный референс.
- Для `The Phone Fund` выполнена дополнительная итерация после QA: банкноты
  с псевдознаками заменены на немаркированные монеты и абстрактный график.
- Финалы используют акцент golden yellow, сохранены рядом с TXT и проверены
  на точность заголовков, отсутствие посторонних цен, сумм и надписей, а также
  технический формат `1024 x 1536`, RGB PNG.

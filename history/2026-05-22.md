# 2026-05-22

## Сессия: Android launcher icon для Nove

- Уточнено назначение `production_mobile/Books/Icon.png`: это иконка приложения/ярлыка, а не картинка для карточек глав.
- Убрано подключение `Books/Icon.png` из `production_mobile/pubspec.yaml` как Flutter asset.
- Убрано использование `Image.asset('Books/Icon.png')` из карточек глав mobile Library и fallback-обложки Workbench.
- Из `production_mobile/Books/Icon.png` сгенерированы Android launcher icons:
  - `android/app/src/main/res/mipmap-mdpi/ic_launcher.png` — 48px
  - `android/app/src/main/res/mipmap-hdpi/ic_launcher.png` — 72px
  - `android/app/src/main/res/mipmap-xhdpi/ic_launcher.png` — 96px
  - `android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png` — 144px
  - `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png` — 192px

## Сессия: Cloud covers для библиотеки Nove

- В `NoveLibraryIndexBuilder` добавлена выгрузка обложек из zip: `manifest.cover` копируется из архива в `assets/library/<level>/<section>/covers/<book>.png`.
- В `library_index.json` добавляется `cover_path`, чтобы мобильная библиотека могла скачать маленькую обложку отдельно от zip книги.
- В `NoveBundledBookRepository` cloud-книги теперь читают `cover_path` и подгружают `coverBytes` по public R2 URL.
- В `pubspec.yaml` добавлены `covers/` asset-папки для всех уровней/разделов.
- Добавлен utility-скрипт `tool/rebuild_nove_library_index.dart` для ручной пересборки локального индекса и cover-файлов.
- Выполнена пересборка `assets/library/library_index.json`: обложки созданы для книг, где `cover.png` есть внутри zip.
- Выполнен `rclone sync` в `r2books:books/nove/library`; публичный `library_index.json` теперь содержит `cover_path`.
- Проверен публичный доступ к одной обложке через R2 Public Development URL: HTTP 200.

## Сессия: Выравнивание chapter cards

- В mobile Library для `_ChapterCard` задана единая фиксированная высота `204`.
- Горизонтальная chapter carousel обёрнута в `SizedBox(height: _ChapterCard.height)`.
- Счётчик `stories` в карточке главы прижат вниз через `Spacer`, чтобы карточки с одно- и двухстрочными названиями были одинаковой высоты.

## Сессия: Разблокировка Load/Open и level tabs

- В cloud listBooks исправлен порядок работы: книги сначала фильтруются по `level`/`section`, и только затем скачивается `cover_path` для реально отображаемых карточек.
- В mobile Library добавлен `loadRequestId`, чтобы устаревшие параллельные refresh-запросы не перетирали актуальное состояние и не держали `_busy`.
- Вкладки уровней `A1/A2/B1/B2/C1` больше не отключаются во время `_busy`; переключение запускает новый актуальный refresh.
- `NoveBookDetailScreen` больше не получает глобальный `_busy` каталога, поэтому `Load/Open/Delete/Favorite` блокируются только во время собственного действия.

## Сессия: Ремонт cloud sync state и удаления книг

- В `NoveBundledBookRepository` добавлен in-memory cache для `library_index.json`, чтобы один экран не скачивал индекс повторно для `chapters` и `more stories`.
- При заданном `NOVE_LIBRARY_BASE_URL` ошибка загрузки cloud index больше не скрывается fallback-ом на локальные assets; теперь выполняются 3 попытки и затем показывается явная ошибка.
- Добавлен in-memory cache обложек `cover_path`, чтобы refresh/переключение не скачивали одну и ту же обложку повторно в рамках жизни экрана.
- В `MobileBookPackageRepository.deletePackage` добавлено удаление по meta внутри `package.json`: если имя папки и `local_book_id` разъехались после старых импортов, реальная папка пакета всё равно удаляется.

## Сессия: Favorite только для скачанных книг

- В mobile Library favorite-флаг на карточках показывается только для установленных (`Loaded`) книг.
- Секция `Favorites` теперь фильтрует не только `_favorites`, но и наличие локально установленного пакета.
- На detail screen кнопка `Add to favorites` отключена, если книга ещё не скачана.

## Сессия: Load без автоперехода в Reader

- После нажатия `Load` книга скачивается и detail screen остаётся открытым.
- Кнопка `Load` на detail screen после успешного скачивания локально переключается в `Open`.
- Переход в Reader теперь выполняется только по явному нажатию `Open`.

## Сессия: Favorites горизонтальной лентой

- В `_ShelfSection` добавлен режим `horizontal`.
- Секция `Favorites` в mobile Library теперь отображает книги горизонтальной прокруткой, а не Wrap-сеткой.
- Остальные секции сохранили прежнее поведение.

## Сессия: Быстрый Open и overflow Favorites

- `MobileBookPackageRepository.markBookOpened` больше не читает/нормализует весь пакет через `readPackage` перед переходом в Reader.
- `markBookOpened` теперь напрямую обновляет только `meta.last_opened_at` в `package.json`, что убирает долгую паузу при первом `Open` после скачивания.
- Высота горизонтальной Favorites-ленты увеличена до `232`, чтобы карточки с title/subtitle не давали bottom overflow.
- Подтверждено по `library_index.json`: `Late for Class` не имеет `cover_path`, поэтому отображается fallback-обложкой цвета темы.

## Сессия: Chapter label в карточках глав

- В карточках глав верхний номер заменён на английскую подпись `Chapter 1`, `Chapter 2`, `Chapter 3`.
- Высота `_ChapterCard` увеличена до `224`, чтобы новая подпись не давала layout overflow.

## Сессия: Repack ready books без TTS

- Расширен существующий Workbench flow обработки библиотеки: если книга уже имеет нужные `reader/dictionary/audio`, но в zip нет cover, а рядом с исходным `.txt` есть cover, Workbench больше не делает простой `Skip ready book`.
- Для такого случая выполняется `Repack ready book`: выбранная книга применяется в форму, вызывается `exportFiles(textOnly: true)`, новая обложка копируется в output, обновляется manifest/load_manifest, пересобирается zip и `library_index.json`.
- `NoveWorkbenchBuilder` теперь в textOnly/re-export режиме использует новую `coverPath`, если она задана, а не только уже существующую cover в output.
- `_WorkbenchExportStatus` расширен флагом `hasCover`; статус берётся и из output-папки, и из установленного zip.
- Repack теперь берёт существующий `book_id` из output/zip manifest и подставляет его перед `exportFiles(textOnly: true)`, поэтому не требует повторного backend import.

## Сессия: Push script для Nove mobile

- Добавлен `production_mobile/workbench/push_nove_mobile_update.bat`.
- Скрипт запускается из папки Workbench, поднимается в корень репозитория и создаёт commit/push для безопасных частей `production_mobile` и `history`.
- В staging включены `production_mobile/lib`, `android`, `test`, `tool`, `assets/library`, `pubspec*`, mobile/workbench bat-файлы и история; `build`, `.dart_tool`, Workbench logs/output не добавляются.

## Сессия: Nove окончательно cloud-only

- Из `production_mobile/pubspec.yaml` убраны bundled `assets/library/...`, поэтому APK больше не тащит локальные zip/covers/index как Flutter assets.
- `NoveBundledBookRepository` переведён в строгий cloud-only режим: без `NOVE_LIBRARY_BASE_URL` каталог считается ошибкой, локальный `rootBundle/assets` fallback удалён.
- При смене языка активная книга переимпортируется только по `cloud:...zip` content hash; старая проверка `assets/library/...zip` удалена из mobile shell.
- Удалён устаревший `NoveContentLibrary` с константами локальной asset-библиотеки.
- `push_nove_mobile_update.bat` больше не добавляет `production_mobile/assets/library` в commit; эта папка остаётся только локальным staging/output для Workbench и R2 sync.

## Сессия: Fix push script для новой папки production_mobile

- В `push_nove_mobile_update.bat` исправлен первый staging шаг: `git add -u` теперь применяется только к `history`, потому что новая untracked папка `production_mobile` ещё не известна Git и давала `pathspec did not match`.
- `production_mobile` по-прежнему добавляется ниже через whitelist (`pubspec`, `lib`, `android`, `test`, `tool`, bat-файлы Workbench/mobile), поэтому основное Lexo-приложение не stage-ится этим скриптом.

## Сессия: GitHub Actions iOS build для Nove

- Добавлен отдельный workflow `.github/workflows/build-nove-ios.yml`.
- Workflow запускается вручную или на push в `main`, если меняется `.github/workflows/build-nove-ios.yml` или `production_mobile/**`.
- Сборка выполняется на `macos-14` в `working-directory: production_mobile`, вызывает `flutter build ios --release --no-codesign` с `NOVE_MODE=mobile` и cloud-only `NOVE_LIBRARY_BASE_URL`.
- Артефакт unsigned IPA выкладывается как `nove-ios-ipa` (`NOVE.ipa`).
- `push_nove_mobile_update.bat` теперь добавляет `.github/workflows/build-nove-ios.yml` и `production_mobile\ios`, чтобы push Nove запускал iPhone-сборку.

## Сессия: Fast-fix iOS Actions build DropdownButtonFormField

- GitHub Actions `build-nove-ios` упал на Flutter 3.32 с ошибкой `No named parameter with the name 'initialValue'` в `nove_workbench_screen.dart`.
- Для совместимости с runner-версией Flutter параметры `DropdownButtonFormField.initialValue` заменены обратно на `value`.
- Локальный Flutter уже помечает `value` как deprecated info, но для текущего GitHub runner это рабочий API и iOS build должен компилироваться.
- Следующий iOS run показал такой же compile error в `mobile_settings_screen.dart`; оставшийся `initialValue` заменён на `value`.
- Поиск `initialValue:` в `production_mobile/lib` больше не находит совпадений.

## Сессия: A1 chapter images в mobile Library

- Из `Books\A1\chapter_images` скопированы первые 5 PNG в `production_mobile/assets/ui/chapters/a1`.
- В `NoveA1Chapter` добавлено опциональное поле `imageAssetPath`.
- Главы 1-5 (`Introduction`, `Family`, `Home & Furniture`, `Food & Drinks`, `Work & Study`) получили asset-картинки.
- Карточки глав в mobile Library теперь показывают `Image.asset` с `BoxFit.cover`; для глав без картинки остаётся старая placeholder-заглушка.
- В полном экране `A1 Chapters` круглые номера заменены на квадратные thumbnail-картинки глав; для глав без картинки остаётся номерная placeholder-заглушка.
- В `pubspec.yaml` добавлен только UI asset path `assets/ui/chapters/a1/`; `assets/library` не возвращался в bundled assets, cloud-only для книг сохранён.
- `push_nove_mobile_update.bat` добавляет `production_mobile\assets\ui`, но не добавляет `production_mobile\assets\library`.

## Сессия: Load dialog задел под выбор языка и голоса

- Добавлен `NoveDownloadOptions` и список будущих Kokoro voice options: `af_heart`, `af_nicole`, `bm_george`, `am_michael`.
- При нажатии `Load` на detail screen теперь открывается диалог `Download book` с выбором `Translation` (`Russian`, `Ukrainian`, `All available`) и `Voice` (`All available` или один из Kokoro voices).
- `NoveBundledBookRepository.importBook` принимает download options, выбирает активный `reader_<lang>.json` по выбранному языку и записывает `download_options`, `selected_target_lang`, `selected_voice_id` в meta локального package.
- Если выбран конкретный голос, которого нет в текущем `tts_manifest`, импорт останавливается с понятной ошибкой `Voice ... is not available in this book package yet`.
- Word audio при импорте больше не хардкодится в `af_heart`, а копируется в папку голоса из `word_audio_manifest.voice_id` с fallback на `af_heart`.

## Сессия: Kokoro voices и Workbench multi-select

- Backend Kokoro profiles расширены до 4 голосов: `af_heart` (`Warm female`), `af_nicole` (`Clear female`), `bm_george` (`British male`), `am_michael` (`American male`).
- В Workbench вместо одного dropdown добавлена панель `Kokoro voices` с чекбоксами.
- Генерация Kokoro package теперь проходит по всем выбранным голосам последовательно с `overwrite=false` и `overwrite_word_audio=false`, чтобы не перезаписывать уже готовое аудио.
- Статус готовности библиотеки теперь проверяет аудио по выбранным voice ids через `tts_manifest` (`profiles/jobs.voice_id`), а не только по факту наличия любых `audio/segments/*.mp3`.
- Для старых zip/output без voice ids сохранён fallback: если аудио есть, но voice id не удалось определить, оно считается `af_heart`.

## Сессия: Обновление A1 chapter images 1-3

- Обновлённые исходники глав 1-3 найдены в `Books\A1\chapter_images` с двойным расширением `.png.png`.
- UI assets перезаписаны с нормальными именами:
  - `assets/ui/chapters/a1/chapter_01_introduction.png`
  - `assets/ui/chapters/a1/chapter_02_family.png`
  - `assets/ui/chapters/a1/chapter_03_home_furniture.png`
- Главы 4-5 не трогались.

## Сессия: Mobile Settings выбор голоса

- В `MobileAppSettings` добавлен `preferredVoiceId` с default `af_heart` и сохранением в `mobile_settings.json`.
- В mobile Settings добавлен dropdown `Reading voice` со списком `af_heart`, `af_nicole`, `bm_george`, `am_michael`.
- `MobileShellScreen` сохраняет выбранный голос через `MobileSettingsRepository` и прокидывает settings в Library.
- `NoveBookDetailScreen` использует выбранный в Settings голос как default в `Download book` dialog.
- Выбор языка в Settings по-прежнему работает как раньше; выбор голоса пока влияет на default при загрузке/импорте книги.

## Сессия: Fix mobile voice switching

- Проверен `book_919621151055_new_student.zip`: `tts_manifest.json` содержит `af_heart`, `af_nicole`, `am_michael`, `bm_george`, а segment audio присутствует по разным `jobId`.
- Причина залипания голоса была в mobile Reader: при открытии книги он предпочитал `activeJob.voice_id` из манифеста и не получал `preferredVoiceId` из Settings.
- `MobileBookPackageMeta` теперь читает `selected_voice_id` из локального package meta.
- `MobileShellScreen` передаёт `preferredVoiceId` в `MobileReaderScreen` и включает его в key reload.
- `MobileReaderScreen` выбирает голос с приоритетом: autoplay request -> Settings voice -> package selected voice -> previous state -> manifest active job -> first profile.
- Удалены неиспользуемые mobile Reader методы старого online TTS generation пути, чтобы analyzer не ругался на dead code.

## Проверка

- `C:\src\flutter\bin\dart.bat format ...` — выполнено.
- `C:\src\flutter\bin\flutter.bat test` — пройдено.
- `C:\src\flutter\bin\dart.bat analyze lib/src/workbench/nove_library_index_builder.dart lib/src/mobile/nove_bundled_book_repository.dart tool/rebuild_nove_library_index.dart` — без замечаний.
- `C:\src\flutter\bin\dart.bat run tool/rebuild_nove_library_index.dart` — пройдено, 6 книг в индексе.
- `rclone sync D:\Programs\LEXO\production_mobile\assets\library r2books:books/nove/library --progress` — пройдено.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после фикса высоты chapter cards.
- `C:\src\flutter\bin\dart.bat analyze lib/src/mobile/nove_bundled_book_repository.dart lib/src/ui/mobile/screens/mobile_reader_catalog_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после разблокировки detail actions и level tabs.
- `C:\src\flutter\bin\dart.bat analyze lib/src/mobile/nove_bundled_book_repository.dart lib/src/mobile/mobile_package_repository.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после ремонта cloud index cache и удаления пакетов.
- `C:\src\flutter\bin\dart.bat analyze lib/src/ui/mobile/screens/mobile_reader_catalog_screen.dart lib/src/ui/mobile/screens/nove_book_detail_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после ограничения favorite только для скачанных книг.
- `C:\src\flutter\bin\dart.bat analyze lib/src/ui/mobile/screens/nove_book_detail_screen.dart lib/src/ui/mobile/screens/mobile_reader_catalog_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после отключения автоперехода в Reader после `Load`.
- `C:\src\flutter\bin\dart.bat analyze lib/src/ui/mobile/screens/mobile_reader_catalog_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после перевода Favorites в горизонтальную ленту.
- `C:\src\flutter\bin\dart.bat analyze lib/src/mobile/mobile_package_repository.dart lib/src/ui/mobile/screens/mobile_reader_catalog_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после ускорения `Open` и фикса высоты Favorites.
- `C:\src\flutter\bin\dart.bat analyze lib/src/ui/mobile/screens/mobile_reader_catalog_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после добавления `Chapter N`.
- `C:\src\flutter\bin\dart.bat analyze lib/src/workbench/nove_workbench_builder.dart lib/src/workbench/nove_workbench_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после добавления repack ready books.
- `C:\src\flutter\bin\dart.bat analyze lib/src/workbench/nove_workbench_screen.dart` — без замечаний после фикса `book_id` для repack.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после фикса `book_id` для repack.
- `C:\src\flutter\bin\dart.bat analyze lib/src/mobile/nove_bundled_book_repository.dart lib/src/ui/mobile/screens/mobile_shell_screen.dart` — без замечаний после перевода mobile на cloud-only.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после перевода mobile на cloud-only.
- `C:\src\flutter\bin\dart.bat format production_mobile\lib\src\workbench\nove_workbench_screen.dart` — выполнено после fast-fix `DropdownButtonFormField`.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\workbench\nove_workbench_screen.dart` — только deprecated info по `value`, без compile errors.
- `C:\src\flutter\bin\dart.bat format production_mobile\lib\src\ui\mobile\screens\mobile_settings_screen.dart` — выполнено после fast-fix `DropdownButtonFormField`.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\ui\mobile\screens\mobile_settings_screen.dart` — только deprecated info по `value`, без compile errors.
- `rg -n "initialValue:" production_mobile\lib` — совпадений нет.
- `C:\src\flutter\bin\dart.bat format production_mobile\lib\src\mobile\nove_a1_chapters.dart production_mobile\lib\src\ui\mobile\screens\mobile_reader_catalog_screen.dart` — выполнено после добавления chapter images.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\mobile\nove_a1_chapters.dart production_mobile\lib\src\ui\mobile\screens\mobile_reader_catalog_screen.dart` — без замечаний.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после добавления chapter images.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\ui\mobile\screens\mobile_reader_catalog_screen.dart` — без замечаний после thumbnail-картинок в `A1 Chapters`.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после thumbnail-картинок в `A1 Chapters`.
- `C:\src\flutter\bin\dart.bat format production_mobile\lib\src\mobile\nove_download_options.dart production_mobile\lib\src\mobile\nove_bundled_book_repository.dart production_mobile\lib\src\ui\mobile\screens\mobile_reader_catalog_screen.dart production_mobile\lib\src\ui\mobile\screens\nove_book_detail_screen.dart` — выполнено после Load dialog.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\mobile\nove_download_options.dart production_mobile\lib\src\mobile\nove_bundled_book_repository.dart production_mobile\lib\src\ui\mobile\screens\mobile_reader_catalog_screen.dart production_mobile\lib\src\ui\mobile\screens\nove_book_detail_screen.dart` — только deprecated info по `DropdownButtonFormField.value`, без compile errors.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после Load dialog.
- `.venv_backend\Scripts\python.exe -m py_compile engine\tts\tts_provider.py` — пройдено после добавления Kokoro voices.
- `C:\src\flutter\bin\dart.bat format production_mobile\lib\src\workbench\nove_workbench_screen.dart production_mobile\lib\src\workbench\nove_workbench_form_panels.dart` — выполнено после Workbench multi-select voices.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\workbench\nove_workbench_screen.dart production_mobile\lib\src\workbench\nove_workbench_form_panels.dart` — только deprecated info по `DropdownButtonFormField.value`, без compile errors.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после Workbench multi-select voices.
- `C:\src\flutter\bin\dart.bat format production_mobile\lib\src\mobile\mobile_settings_repository.dart production_mobile\lib\src\ui\mobile\screens\mobile_settings_screen.dart production_mobile\lib\src\ui\mobile\screens\mobile_shell_screen.dart production_mobile\lib\src\ui\mobile\screens\mobile_reader_catalog_screen.dart production_mobile\lib\src\ui\mobile\screens\nove_book_detail_screen.dart` — выполнено после Settings voice dropdown.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\mobile\mobile_settings_repository.dart production_mobile\lib\src\ui\mobile\screens\mobile_settings_screen.dart production_mobile\lib\src\ui\mobile\screens\mobile_shell_screen.dart production_mobile\lib\src\ui\mobile\screens\mobile_reader_catalog_screen.dart production_mobile\lib\src\ui\mobile\screens\nove_book_detail_screen.dart` — только info, без compile errors.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после Settings voice dropdown.
- `C:\src\flutter\bin\dart.bat analyze production_mobile\lib\src\mobile\mobile_package_models.dart production_mobile\lib\src\ui\mobile\screens\mobile_reader_screen.dart production_mobile\lib\src\ui\mobile\screens\mobile_shell_screen.dart` — без замечаний после фикса выбора голоса.
- `C:\src\flutter\bin\flutter.bat test` — пройдено после фикса выбора голоса.

# 2026-04-07

## Сессия: MVP16 local mobile cards + manual sync + Settings refocus

### Что сделано

- добавлено локальное mobile-хранилище карточек:
  - `app/lib/src/mobile/mobile_cards_repository.dart`
  - карточки теперь живут отдельно от книги
  - добавлены поля `device_id`, `source_unit_id`, `updated_at`, `deleted_at`, `sync_state`
- обновлены Dart-модели карточек:
  - `app/lib/src/cards_models.dart`
  - единый payload теперь подходит и для desktop API, и для local mobile cards storage
- расширены mobile settings:
  - `app/lib/src/mobile/mobile_settings_repository.dart`
  - добавлены `device_id` и `last_sync_at`
- mobile reader переведён на локальное сохранение карточек:
  - `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart`
  - сохранение через detail sheet больше не зависит от host endpoint `/reader/detail-sheet/save`
- mobile cards list / review переведены на локальный источник данных через callbacks:
  - `app/lib/src/screens/cards_list_screen.dart`
  - `app/lib/src/screens/card_review_screen.dart`
- mobile shell перепривязан:
  - `app/lib/src/ui/mobile/screens/mobile_shell_screen.dart`
  - убраны import/update действия из mobile `Settings`
  - добавлена кнопка `Синхронизировать`
  - добаван показ `Последняя синхронизация`
  - добаван показ pending changes count
- mobile settings screen переработан под `Host + Sync`:
  - `app/lib/src/ui/mobile/screens/mobile_settings_screen.dart`
  - блок загрузки книги убран
  - блок sync добавлен
- desktop backend расширен под manual sync:
  - `engine/api.py`
  - новый endpoint `POST /mobile/sync/full`
- desktop storage переведён на tombstone-модель для карточек:
  - `engine/storage.py`
  - `saved_cards` теперь хранит `device_id`, `updated_at`, `deleted_at`
  - удаление карточки больше не физическое, а tombstone
  - list/review фильтруют удалённые карточки
  - review обновляет `updated_at`
  - добавлена merge-логика для mobile sync по `id/updated_at/deleted_at`

### Проверки

- `python3 -m py_compile engine/storage.py engine/api.py` — OK
- smoke-test на временном storage:
  - insert через `sync_mobile_cards_full` — OK
  - review update — OK
  - tombstone delete — OK
  - merge-case: tombstone побеждает более старую живую запись — OK

### Ограничения / заметки

- Dart formatter/analyzer не удалось запустить из этой среды:
  - вызов `dart` и `dart.exe` упирается в системную ошибку WSL `UtilBindVsockAnyPort: socket failed 1`
- файлы `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart` и `engine/storage.py` уже превышают желаемый размер;
  - при следующем проходе стоит вынести sync/cards-логику в отдельные модули

## Дополнительный шаг: review loop + player controls + mobile UI cleanup

### Что сделано

- review-сессия больше не заканчивается автоматически:
  - `app/lib/src/screens/card_review_screen.dart`
  - после swipe карточка обновляется и уходит в конец очереди
  - цикл остаётся бесконечным, пока пользователь сам не выйдет
- в review снова участвуют все карточки для круга, включая уже выученные:
  - `app/lib/src/mobile/mobile_cards_repository.dart`
  - `engine/storage.py`
- из списка карточек убран перевод:
  - `app/lib/src/screens/cards_list_screen.dart`
  - в списке остаётся только оригинал и progress strip
- mobile playback bar расширен:
  - `app/lib/src/widgets/reader_playback_bar.dart`
  - добавлена кнопка `+1`
  - добавлена кнопка скорости
  - tap по скорости сбрасывает на `1x`
  - long press открывает выбор speed
- mobile reader UI упрощён:
  - `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart`
  - убрана кнопка settings из reader app bar
- mobile library UI упрощён:
  - `app/lib/src/ui/mobile/screens/mobile_library_screen.dart`
  - убран refresh из app bar
  - убраны строки `eng -> rus` и `Позиция N`
- mobile settings отвязаны от текущей книги:
  - `app/lib/src/ui/mobile/screens/mobile_settings_screen.dart`
  - больше не показываются название книги и подпись `Текущая книга`

### Проверки

- `python3 -m py_compile engine/storage.py engine/api.py` — OK
- `git diff --check` по затронутым файлам — OK

## Дополнительный шаг: full sync теперь тянет и mobile books

### Что сделано

- `Settings -> Синхронизировать` больше не ограничен карточками:
  - `app/lib/src/ui/mobile/screens/mobile_shell_screen.dart`
  - после `cards sync` теперь выполняется pull desktop library
  - для каждой host-книги скачивается `mobile book package`
- добавлен поиск существующей локальной книги по:
  - `desktop_book_id`
  - fallback: `content_hash`
  - файл: `app/lib/src/mobile/mobile_package_repository.dart`
- при обновлении package сохраняется локальная mobile-идентичность:
  - сохраняется существующий `local_book_id`
  - сохраняется `current_paragraph_index`
  - сохраняется `last_opened_at`
- итог:
  - sync теперь подтягивает книги и их packaged reader data
  - вместе с package приезжают `reader_payload`, words/alignment payload и `tts_manifest`
  - локально не теряется позиция чтения

### Проверки

- `python3 -m py_compile engine/storage.py engine/api.py` — OK
- `git diff --check` по `mobile_package_repository.dart` и `mobile_shell_screen.dart` — OK

## Дополнительный шаг: Android launcher теперь сохраняет logcat при падении app

### Что сделано

- обновлён `scripts/run_lexo_mobile_android_inner.bat`
- перед запуском Flutter очищается adb logcat buffer
- если `flutter run` падает, launcher автоматически сохраняет:
  - `logs/android_logcat.txt`
- это нужно для случаев, когда bat-окно закрывается слишком быстро и пользователь не успевает увидеть runtime-crash

## Дополнительный шаг: Android wrapper больше не закрывается сразу

### Что сделано

- обновлён `run_lexo_mobile_android.bat`
- после завершения запуска окно теперь всегда остаётся открытым на `pause`
- это позволяет спокойно увидеть:
  - `Exit code`
  - путь к `run_lexo_mobile_android.log`
  - сообщение об ошибке launcher-а

## Дополнительный шаг: Android wrapper пишет в уникальный log-файл на каждый запуск

### Что сделано

- обновлён `run_lexo_mobile_android.bat`
- лог больше не пишется в один и тот же `run_lexo_mobile_android.log`
- теперь используется файл вида:
  - `logs/run_lexo_mobile_android_YYYY-MM-DD_HHMMSS.log`
- это убирает проблему `The process cannot access the file because it is being used by another process`

## Дополнительный шаг: добавлены debug-логи для mobile books sync

### Что сделано

- обновлён `app/lib/src/ui/mobile/screens/mobile_shell_screen.dart`
- добавлены debug-логи `LEXO_SYNC` для этапов:
  - старт sync
  - count merged cards
  - count desktop library books
  - pull каждой host-книги
  - выбранный `local_book_id` при save
  - итоговый размер local mobile library после sync

## Дополнительный шаг: sync-логи теперь пишутся в локальный файл приложения

### Что сделано

- добавлен `app/lib/src/mobile/mobile_sync_debug_logger.dart`
- `mobile shell` теперь пишет sync-диагностику не только в runtime log, но и в persistent file
- файл внутри mobile app documents:
  - `mobile_sync_debug.log`
- это нужно для случаев, когда `flutter run`/`adb` теряет debug-сессию и консольные логи не успевают сохраниться

## Дополнительный шаг: mobile sync debug теперь печатается прямо в engine console

### Что сделано

- в `engine/api.py` добавлен endpoint:
  - `POST /mobile/debug/log`
- mobile logger теперь отправляет sync-диагностику в host:
  - `app/lib/src/api/api_client.dart`
  - `app/lib/src/mobile/mobile_sync_debug_logger.dart`
- engine печатает эти сообщения прямо в консоль ПК в формате:
  - `[LEXO MOBILE_SYNC] ...`
- внутренний app file log оставлен как fallback, но основной удобный канал теперь — host console

## Дополнительный шаг: фикс sync книг после package-response ошибки

### Что сделано

- в `engine/storage.py` desktop library теперь отдаёт `content_hash` для книг:
  - mobile sync больше не работает с пустым `remote_hash`
- чтение source text и вычисление content hash вынесено в отдельные helper-методы
- в `app/lib/src/api/api_client.dart` чтение JSON-ответов сделано устойчивым к битому UTF-8:
  - ответ сначала читается как bytes
  - при `FormatException` используется `utf8.decode(..., allowMalformed: true)`
  - в runtime log пишется `UTF8_FALLBACK`
- это нужно для кейса, когда `/mobile/books/package` доходит до mobile, но package sync падает на декодировании ответа

### Проверки

- `python3 -m py_compile engine/storage.py engine/api.py` — OK
- `git diff --check` по `engine/storage.py` и `app/lib/src/api/api_client.dart` — OK

## Дополнительный шаг: фикс transport-обрыва на `/mobile/books/package`

### Что сделано

- в `app/lib/src/api/api_client.dart` mobile-запросы переведены на отдельный `HttpClient` на каждый запрос
- для mobile HTTP-запросов отключён persistent connection:
  - `request.persistentConnection = false`
  - header `Connection: close`
- в `engine/api.py` server-ответы теперь явно отдают:
  - header `Connection: close`
  - `flush()` после записи body
  - `close_connection = True`
- для `/mobile/books/package` добавлен server-side лог размера ответа:
  - `[LEXO ENGINE] MOBILE_PACKAGE book_id=... bytes=...`

## Дополнительный шаг: sync audio reconciliation + безопасный fallback playback

### Что сделано

- обновлён `app/lib/src/ui/mobile/screens/mobile_shell_screen.dart`
  - `Settings -> Синхронизировать` теперь не валит весь book sync из-за одного битого audio segment
  - для TTS audio добавлена сверка:
    - `expected`
    - `present`
    - `downloaded`
    - `failed`
  - ошибки по отдельным сегментам теперь логируются как `SYNC_BOOK_AUDIO_SEGMENT_ERROR`
  - итог по книге теперь пишется даже при partial audio failures
- обновлён `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart`
  - local/offline playback больше не включается только по одному факту наличия `tts_manifest`
  - перед offline playback теперь проверяется наличие локальных audio files для выбранного job
  - если локального сегмента нет, но host доступен, reader делает host fallback download вместо мгновенной ошибки offline playback

### Зачем

- это нужно для кейса, когда package книги уже сохранён локально, но часть `.wav` сегментов не докачалась из-за transport-ошибки
- раньше reader видел `tts_manifest`, уходил в offline playback и падал на первом отсутствующем локальном audio file
- теперь sync работает как reconcile, а reader не считает книгу offline-ready раньше времени

## Дополнительный шаг: Android launcher теперь стартует AVD в safer boot mode

### Что сделано

- обновлён `scripts/run_lexo_mobile_android_inner.bat`
  - launcher теперь стартует AVD с `-no-snapshot-load`
  - это уменьшает зависимость от битого Quick Boot snapshot
  - увеличен таймаут ожидания появления `emulator-*` в `adb devices`
  - загрузка Android теперь считается завершённой только когда:
    - `sys.boot_completed=1`
    - и `init.svc.bootanim=stopped`
  - при таймауте launcher теперь пишет в лог, что проблема вероятнее всего в AVD startup state, и подсказывает:
    - `Cold Boot Now`
    - `Wipe Data`
    - `RAM = 4096 MB`

### Зачем

- это нужно для кейса, когда запуск через `bat` зависает ещё до старта Flutter
- по логам проблема была в нестабильном старте AVD, а не в коде LEXO

## Дополнительный шаг: упрощён binary transport для `/mobile/books/audio`

### Что сделано

- обновлён `engine/api.py`
  - для `GET /mobile/books/audio` убрана кастомная chunked-подача файла через ручной stream iterator
  - `.wav` теперь читается целиком в память и отправляется как обычный body с `Content-Length`
  - лог `RESPONSE_OK FILE ... bytes=.../...` сохранён

### Зачем

- это нужно для кейса, когда сервер логировал успешную полную отдачу audio segment, но Android client всё равно получал:
  - `HttpException: Connection closed while receiving data`
- гипотеза в том, что проблема была в серверной подаче бинарного body, а не в самих `.wav` файлах или в sync-логике

## Дополнительный шаг: client audio download на Android теперь с retry и без forced close

### Что сделано

- обновлён `app/lib/src/api/api_client.dart`
  - `downloadTtsAudio()` больше не выставляет вручную:
    - `request.persistentConnection = false`
    - `Connection: close`
  - для audio download добавлен retry:
    - до 3 попыток на один segment
  - добавлены runtime-логи:
    - `GET ... attempt=N`
    - `AUDIO_RETRY ...`
    - `AUDIO_OK ... bytes=...`

### Зачем

- это нужно для кейса, когда server-side audio endpoint уже логировал `RESPONSE_OK FILE`, но Android client всё ещё нестабильно рвал соединение на бинарном body
- retry нужен как страховка для нестабильного transport-а на emulator/Android HttpClient

## Дополнительный шаг: Android app теперь запускается через adb reverse + 127.0.0.1

### Что сделано

- обновлён `scripts/run_lexo_mobile_android_inner.bat`
  - launcher теперь выполняет:
    - `adb reverse tcp:8765 tcp:8765`
  - Flutter app запускается с:
    - `--dart-define=LEXO_BASE_URL=http://127.0.0.1:8765`
  - в лог launcher-а теперь печатается выбранный mobile base URL

### Зачем

- это нужно как прямой эксперимент для проверки гипотезы, что бинарные audio responses ломаются именно на маршруте через emulator host alias `10.0.2.2`
- после `adb reverse` Android app ходит к engine через `127.0.0.1:8765` внутри эмулятора, минуя этот маршрут

### Зачем

- это нужно для кейса, когда small JSON endpoints проходят, а большой `book package` рвётся на Android с ошибкой:
  - `HttpException: Connection closed while receiving data`

### Проверки

- `python3 -m py_compile engine/api.py` — OK
- `git diff --check` по `engine/api.py` и `app/lib/src/api/api_client.dart` — OK

## Дополнительный шаг: sync debug теперь виден прямо в Settings и копируется кнопкой

### Что сделано

- `app/lib/src/mobile/mobile_sync_debug_logger.dart`
  - logger больше не молча проглатывает ошибку доставки remote debug-log
  - последняя ошибка отправки сохраняется в `debugReport`
  - в памяти держатся последние строки sync-диагностики
- `app/lib/src/ui/mobile/screens/mobile_shell_screen.dart`
  - sync debug report пробрасывается в `Settings`
  - добавлено копирование debug report в clipboard
- `app/lib/src/ui/mobile/screens/mobile_settings_screen.dart`
  - добавлен блок `Sync Debug`
  - добавлена кнопка `Копировать`
  - debug text показывается как selectable text

### Проверки

- `git diff --check` по `mobile_sync_debug_logger.dart`, `mobile_settings_screen.dart`, `mobile_shell_screen.dart` — OK

## Дополнительный шаг: speed control унифицирован по `TtsLevel` на Windows и mobile

### Что сделано

- `app/lib/src/screens/reader_screen.dart`
  - speed button в player теперь открывает реальный picker
  - picker строится только из backend `TtsLevel`
  - player speed больше не является отдельной скрытой логикой от `TtsPanel`
- `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart`
  - убран hardcoded список скоростей
  - убрана отдельная локальная переменная `_playbackSpeed`
  - speed button теперь использует только `_state.ttsLevels`
  - offline mobile reader берёт `profiles/levels` из local package как baseline, а remote TTS только переопределяет их при доступности host
- на обеих платформах label скорости теперь вычисляется из выбранного `TtsLevel.playbackSpeed`

### Проверки

- `rg` не находит старый hardcoded speed list и пустой `onSpeedLongPress`
- `git diff --check` по `mobile_reader_screen.dart` и `reader_screen.dart` — OK

## Дополнительный шаг: mobile sync теперь подтягивает TTS audio, а reader умеет offline playback

### Что сделано

- `app/lib/src/ui/mobile/screens/mobile_shell_screen.dart`
  - после sync book package mobile теперь скачивает готовые wav-сегменты для `ready` TTS jobs
  - добавлен debug log:
    - `SYNC_BOOK_AUDIO desktop_id=... local_id=... downloaded=N`
- `app/lib/src/mobile/mobile_package_models.dart`
  - package теперь парсит `tts_manifest.jobs`
  - строится локальный `TtsState`
  - сохраняется map `segmentsByJobId`
- `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart`
  - mobile reader использует package TTS state как offline fallback
  - playback переключается на локальный режим, если для job уже есть package segments/audio
  - pause/resume/prev/next/stop работают локально без host в режиме offline playback

### Проверки

- `git diff --check` по `mobile_package_models.dart`, `mobile_reader_screen.dart`, `mobile_shell_screen.dart` — OK

## Дополнительный шаг: фикс Windows TXT import при запуске engine из WSL

### Что сделано

- `engine/storage.py`
  - import TXT теперь нормализует Windows filesystem paths в WSL mount path
  - поддержаны форматы:
    - `D:\...`
    - `file:///D:/...`
    - обычный `/mnt/d/...`
- это устраняет ошибку:
  - `TXT file not found: D:\Programs\LEXO\Books\...`
  - когда Windows UI передаёт путь в host, а сам Python engine работает в WSL

### Проверки

- `python3 -m py_compile engine/storage.py` — OK
- `git diff --check` по `engine/storage.py` — OK
- локальная проверка нормализации:
  - `D:\...` -> `/mnt/d/...`
  - `file:///D:/...` -> `/mnt/d/...`

## Дополнительный шаг: desktop TXT import больше не зависит от filesystem path

### Что сделано

- `engine/api.py`
  - добавлен endpoint `POST /books/import-text`
- `app/lib/src/api/api_client.dart`
  - добавлен `importDesktopBookText(...)`
  - mobile import API переименован в `importMobileBookText(...)`
- `app/lib/src/ui/desktop/screens/desktop_shell_screen.dart`
  - desktop settings import теперь читает содержимое TXT через `File(...).readAsString()`
  - в engine отправляются `title + source_text`, а не `source_path`
- `app/lib/src/screens/home_screen.dart`
  - library import переведён на ту же модель
- `app/lib/src/features/library/library_feature.dart`
  - добавлен `importBookText(...)`

### Зачем

- это убирает класс ошибок вида:
  - `TXT file not found: D:\...`
- desktop import теперь не зависит от того, в каком filesystem namespace живёт engine process

### Проверки

- `python3 -m py_compile engine/api.py` — OK
- `git diff --check` по затронутым файлам — OK

## Дополнительный шаг: усилен transport backend для mobile audio/sync

### Что сделано

- `engine/api.py`
  - отдача `JSON` и `audio/wav` переведена на единый write-loop с явной диагностикой записи body
  - `audio` теперь отдаётся потоково по чанкам, а не через `read_bytes()` целиком
  - для `/mobile/books/audio` добавлен лог:
    - `MOBILE_AUDIO book_id=... job_id=... segment_index=... path="..." bytes=...`
  - для всех ответов добавлены логи:
    - `RESPONSE_OK ... bytes=sent/total`
    - `RESPONSE_ABORTED ... bytes=sent/total error=...`
  - аварийная отправка error-response сделана безопаснее через `_try_send_json(...)`

### Зачем

- это нужно для диагностики и снижения риска transport-обрыва на Android/mobile sync:
  - `HttpException: Connection closed while receiving data`
- теперь видно, сервер завершает отправку полностью или соединение рвётся во время write

### Проверки

- `python3 -m py_compile engine/api.py` — OK

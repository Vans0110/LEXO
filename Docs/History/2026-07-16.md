# 2026-07-16

## Единый reader lexicon

- Книжные `dictionary_<lang>.json` и `word_to_word_<lang>.json` объединены в
  один `reader_lexicon_<lang>.json` на книгу и язык перевода.
- Контракт v3 содержит `words`, `phrases`, `word_alignments` и
  `phrase_alignments`: словарные записи хранятся один раз, occurrence-alignments
  ссылаются на `dictionary_key`/`phrase_key` и больше не дублируют массивы
  `translations` и phrase components.
- `words` строится только из Global Words, `phrases` — только из Global Phrases;
  в lexicon попадает книжный срез из реально используемых 81 word key и 5 phrase
  key для `The Wrong Classroom`. Reader задаёт occurrence и target-span, а
  book-layer audit используется только для absorbed ownership.
- Workbench build/refresh, load manifest и backend artifact refresh переведены на
  `reader_lexicon_<lang>.json`. Новые ZIP удаляют legacy dictionary/word-to-word;
  mobile loader сохраняет read-fallback для уже скачанных старых пакетов.
- Пересобраны 10 RU/UK книжных ZIP. В `The Wrong Classroom` проверены 81 word,
  5 phrases, 146 word-alignments и 5 phrase-alignments; старые файлы в ZIP
  отсутствуют.
- Проверки: backend reader-lexicon tests 3/3, Flutter interaction/runtime tests
  10/10, `flutter analyze` без новых ошибок, debug APK установлена на
  `emulator-5554`; live Reader сохранил `into Room → в комнату` и отдельные
  словарные карточки.

## Глобальный словарь служебных слов

- Создан отдельный RU Global `global_function_words_ru.json` с краткими
  объяснениями для артиклей, форм `be`, `do`, `have`, `to`, `there` и `of`.
- Переводы не дублируются: они по-прежнему всегда берутся из Global Words.
  Новый Global хранит только роль формы и понятное объяснение.
- В `reader_lexicon_ru.json` добавляется книжный срез `function_words` только
  для реально встречающихся форм. Для `The Wrong Classroom` это `the`, `a`,
  `am`, `is`, `are`, `there` и `of`.
- Reader показывает объяснение внутри соответствующей вертикальной карточки
  Words. Global Phrases и логика фраз не изменялись.
- Пересобраны 10 RU/UK ZIP; Flutter interaction tests — 10/10.

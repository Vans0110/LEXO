# 2026-06-22

## Адаптивная сетка книг главы

- Исправлен экран книг главы: карточки больше не имеют фиксированную ширину
  116 px внутри адаптивной сетки.
- При доступной ширине меньше 520 px используются две колонки, карточки
  равномерно заполняют строку; от 520 px используются три колонки.
- Ширина карточки ограничена 180 px, сетка центрируется, а неполная последняя
  строка остаётся выровненной слева внутри сетки.
- Обложки динамических карточек сохраняют пропорцию 2:3. Горизонтальные полки
  `Favorites` и `Downloaded Books` сохраняют прежний размер 116 x 162.
- Добавлены widget-тесты двухколоночной сетки шириной 300 px и трёхколоночной
  сетки шириной 600 px; точечный Dart analyze и все три теста проходят.
## Исправление WALKER в A Voice from Online

- В каноническом корпусе и исходном TXT написание `W-A-L-K-E-R` заменено на `WALKER`.
- Словарная статья `w-a-l-k-e-r|PROPN` заменена на `walker|PROPN` в Virgil Core и book seed; RU/UK словари книги пересобраны с переводами `Уокер` / `Вокер`.
- В БД и экспортированных reader/word-to-word JSON обновлён один сегмент книги (индекс 9).
- Для основного голоса `af_heart` заново создан этот сегмент в вариантах `base` и `slow_native`; остальные голоса и сегменты аудио не перегенерировались.
- Проверены все исходные TXT книг и A1 Corpus: других побуквенных написаний через дефисы, пробелы или точки не найдено.

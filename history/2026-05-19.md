# 2026-05-19

## Сессия: старт отдельного production mobile приложения Nove

- Создана отдельная папка `production_mobile` как копия текущего Flutter `app`.
- Новый продукт переименован в `Nove`: обновлены `pubspec.yaml`, Android label, namespace и applicationId.
- `Nove` переведён на mobile-only entry: desktop shell больше не выбирается через ширину окна.
- Новый mobile shell очищен от видимого Host/Sync контура: оставлены Reader и Cards.
- Добавлен Reader-каталог с разделами:
  - `Избранное`
  - `Главы`
  - `Книги`
- Smoke test перенесён на новый Reader-каталог и проверяет базовые секции Nove.
- В `production_mobile` добавлен `run_nove_mobile_android.bat` для запуска Nove на Android emulator по схеме основного LEXO launcher.
- Нижняя навигация Nove изменена на две вкладки: `Reader` и `Library`; каталог `Избранное / Главы / Книги` перенесён во вкладку `Library`.
- Добавлен каркас bundled-библиотеки Nove: `assets/library/a1/chapters` и `assets/library/a1/more_a1_stories` с подпапками `books_zip`, `json`, `tts`; пути зарегистрированы в `pubspec.yaml` и `NoveContentLibrary`.
- Создан `production_mobile/workbench` и desktop `NoveWorkbenchScreen`: загрузка TXT, импорт в backend, запуск Kokoro package generation, экспорт `reader/dictionary/tts/word_audio/word_to_word` json и audio-файлов в `workbench/output`.
- Добавлен `production_mobile/workbench/run_nove_workbench.bat` для запуска desktop Workbench одним кликом.
- Режимы запуска Nove разведены через `NOVE_MODE`: Workbench принудительно `windows/workbench`, Android launcher принудительно `mobile`.
- `run_nove_workbench.bat` очищает generated `.plugin_symlinks` перед Windows запуском, чтобы обходить Flutter `PathExistsException` на stale plugin link.
- `run_nove_workbench.bat` также очищает `build/windows`, потому что копия `production_mobile` унаследовала CMake cache из старого `app` пути.
- `run_nove_workbench.bat` переведён на live console output вместо полного redirect в log, потому что `flutter run` держит процесс открытым и окно выглядело зависшим.
- Исправлен Workbench TTS voice contract: генерация отправляет `TtsProfile.voiceId` (`af_heart`), а не profile id (`kokoro_af_heart`); status panel теперь показывает `error_message`.
- Workbench export теперь создаёт `.zip` из готовой output-папки и копирует его в `assets/library/<level>/<section>/books_zip/`.
- Fast-fix zip export: PowerShell `Compress-Archive` получает source/destination через environment variables, чтобы `DestinationPath` не становился пустым.
- Workbench launcher разделён на быстрый `run_nove_workbench.bat` без очистки `build/windows` и аварийный `run_nove_workbench_clean.bat` для полной Windows пересборки.
- Добавлен mobile loader bundled zip: Nove показывает zip-книги из `assets/library/a1/chapters/books_zip`, импортирует их в локальную `mobile_library` и открывает через текущий reader.
- Workbench export теперь очищает output-папку перед пересборкой, чтобы stale audio не попадало в zip.
- Исправлены mobile-вкладки Nove: `Library` первая, `Reader` вторая, `Cards` третья; bundled zip loader переведён на `AssetManifest.loadFromAssetBundle`.
- Library UI переведён на обложки книг: zip-книги и локальные книги показываются карточками с cover placeholder, названием и статусом загрузки.
- Добавлен экран деталей книги с увеличенной обложкой и действиями `Загрузить` / `Открыть`, `Добавить в избранное` / `Убрать из избранного`, `Удалить книгу`.
- Избранное сделано ручным и автономным через `nove_favorites.json`; книги не попадают туда автоматически после загрузки.
- В Library восстановлена отдельная полка `More A1 Истории` рядом с `A1 Главы`.
- Убрана дублирующая полка `Книги`: загруженная zip-книга остаётся в своём разделе (`A1 Главы` / `More A1 Истории`) и получает статус `Загружена`.
- В Workbench добавлен блок обложки: выбор JPG/PNG, preview, запись `cover.jpg`/`cover.png` в output/zip и поле `cover` в manifest/load manifest.
- Mobile Library теперь читает обложку из bundled zip и показывает её на карточке книги и экране деталей; если обложки нет, остаётся placeholder `Nove`.
- Добавлены 12 A1-глав как отдельные полки Library и выбор главы в Workbench; export пишет `chapter_id` / `chapter_title` в manifest, старые zip без главы показываются в `Глава 1 — Introduction`.
- Fast-fix Workbench build: убран `const` со списка `DropdownMenuItem` для A1-глав, потому что Dart не принимает `for` внутри constant expression.
- Library получила верхние вкладки уровней `A1/A2/B1/B2/C1`; главы переведены в горизонтальную карусель карточек с placeholder-местом под изображение, а книги выбранной главы показываются ниже.
- Workbench level selector расширен до `A1/A2/B1/B2/C1`; добавлены пустые asset-папки и `pubspec.yaml` paths для новых уровней.
- Library UI доработан по макету: нижняя полка книг выбранной главы убрана с главного экрана, тап по главе открывает отдельный список книг, в `Chapters` добавлена кнопка `All`, level-кнопки сделаны без галочек и с центрированным скруглённым видом.
- Созданы локальные рабочие папки глав в `production_mobile/Books/A1`: 12 папок `Глава 1 - Introduction` ... `Глава 12 - Travel & Plans`.
- Обсужден перенос библиотеки книг в Cloudflare R2 bucket `books` с зеркалом структуры `assets/library` под ключом `nove/library/...`.
- На Windows установлен `rclone v1.74.1` через `winget`; создан remote `r2books` типа `s3` provider `Cloudflare`, endpoint `https://2a082fb8bde0c958602740e10df9e03c.r2.cloudflarestorage.com`.
- R2 sync выполнен вручную: `rclone sync D:\Programs\LEXO\production_mobile\assets\library r2books:books/nove/library --progress` загрузил 37 файлов, 49.540 MiB; `rclone ls r2books:books/nove/library` подтвердил zip-книги и `library_index.json`.
- Важно: R2 `Secret Access Key` был случайно отправлен в чат. После проверки синка нужно удалить этот API token в Cloudflare, создать новый `Object Read & Write` token для bucket `books` и обновить remote `r2books` через `rclone config`.

## Проверка

- `C:\src\flutter\bin\dart.bat format ...` — выполнено для изменённых Dart-файлов.
- `C:\src\flutter\bin\flutter.bat test` — пройдено.
- `C:\src\flutter\bin\flutter.bat analyze` — без новых ошибок компиляции, но команда возвращает non-zero из-за существующих warning/info в скопированной базе.

# 2026-04-23

## Сессия: Доводка MVP21 до консистентного source-first runtime path

### Что сделано

- обновлён [engine/storage.py](/mnt/d/Programs/LEXO/engine/storage.py)
  - `get_detail_sheet()` больше не пересобирает `v2` через debug path
  - detail теперь читает сохранённый contour из `segments`:
    - `source_analysis_json`
    - `source_lookup_json`
    - `source_coverage_json`
    - `source_effective`
  - `sheet_translation_text` теперь может брать backend-prepared `source_first_focus_text` как primary semantic focus
  - добавлен `rebuild_book_source_first(...)`
    - пересчитывает `source-first` поля по уже существующим `source_text/target_text`
    - не требует повторного перевода книги

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - coordination detector больше не склеивает ложные группы через predicate token
  - object detector перестал тащить predicate в object list
  - punctuation исключена из `nounish` / `modifier` path

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - добавлен отдельный formula fallback для phrase-group вроде:
    - `Good morning -> Доброе утро`
  - object-tail fallback больше не используется для formula как для обычной noun phrase

- обновлены [app/lib/src/detail_sheet_models.dart](/mnt/d/Programs/LEXO/app/lib/src/detail_sheet_models.dart) и [engine/storage.py](/mnt/d/Programs/LEXO/engine/storage.py)
  - source-first unit selection переведён на более устойчивый порядок:
    - time-special case
    - exact source-text match
    - compacted non-punctuation order
    - только потом raw token index
  - это убрало часть ложных попаданий в `:`, `,`, `"` при tap/detail

- обновлён [scripts/analyze_translation_segmentation.py](/mnt/d/Programs/LEXO/scripts/analyze_translation_segmentation.py)
  - отчёт теперь показывает не только legacy word alignment, но и:
    - `analysis_version`
    - `segment_type`
    - `translation_kind`
    - `source_first_units`
    - `source_first_groups`
    - per-word `source_first` focus/effective coverage
  - selection в отчёте выровнен с runtime path

### Что проверено

- `python3 -m py_compile engine/storage.py engine/v2_core/source_analyzer.py engine/v2_core/coverage.py scripts/analyze_translation_segmentation.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- `LEXO_TRANSLATOR=mock python3 -c "... LexoStorage(...).rebuild_book_source_first('book_8763c2e6199d') ..."`
  - persisted `source-first` contour книги `The Sunny Morning` пересобран для `36` сегментов
- `python3 scripts/analyze_translation_segmentation.py --paragraph 2 --format text`
  - подтверждено, что отчёт теперь показывает runtime-oriented `source-first` картину, а не только legacy spans

### Что это дало

- `reader`, `detail` и анализатор книги теперь ближе к одному источнику истины по `source-first`
- detail больше не живёт на отдельной on-the-fly версии `v2`
- из runtime path убрано несколько заметных ложных semantic выборов:
  - punctuation selected as unit
  - ложные `coordination` группы вроде `kitchen and sees`
  - formula fallback, растягивающий `Good morning` до `утро, Луна`

## Сессия: Доводка meta/time coverage и дальнейшее сужение legacy-роли

### Что сделано

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - `META` unit теперь несёт `meta_kind` в `metadata`:
    - `chapter`
    - `time`
    - `punctuation`
  - `chapter` больше не попадает в обычную lexical-ветку

- обновлён [engine/v2_core/lookup.py](/mnt/d/Programs/LEXO/engine/v2_core/lookup.py)
  - `_meta_translation(...)` теперь умеет одиночные meta-token cases:
    - `AM -> утра`
    - `PM -> вечера`

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - добавлен отдельный `_resolve_meta_coverage(...)`
  - `Chapter` теперь может получать direct meta coverage вместо `fuzzy`
  - `7:00` теперь даёт числовой target coverage
  - `AM` теперь даёт `exact -> утра`

- повторно выполнен `rebuild_book_source_first(...)` для книги `The Sunny Morning`
  - persisted `source-first` contour заново пересчитан после meta/time правок

### Что проверено

- `python3 -m py_compile engine/v2_core/source_analyzer.py engine/v2_core/coverage.py engine/v2_core/lookup.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite снова прошёл `PASS`
- ручная debug-проверка показала:
  - `7:00 -> exact -> 7`
  - `AM -> exact -> утра`
  - `Chapter -> exact -> Глава`

### Что осталось как текущие ограничения

- legacy `word_alignment.py` всё ещё остаётся в runtime и ещё не выведен в чистый fallback-only слой
- ручные runtime словари в legacy alignment всё ещё существуют:
  - `ANCHOR_TRANSLATIONS`
  - `REPORTING_VERB_TRANSLATIONS`
- grammar/direct hint слой в [engine/lexical_enrichment.py](/mnt/d/Programs/LEXO/engine/lexical_enrichment.py) всё ещё использует curated mappings:
  - `DIRECT_MEANINGS`
  - `GRAMMAR_HINTS`
- time/meta coverage (`7:00 AM`) и часть lexical fuzzy-case ещё требуют отдельной доработки, если переводить весь runtime полностью на `source-first`

## Сессия: Runtime/detail переведены на source-first primary с legacy как fallback

### Что сделано

- обновлён [engine/storage.py](/mnt/d/Programs/LEXO/engine/storage.py)
  - `detail source_first.selected_unit` теперь выбирается тем же helper-путём, что и runtime, без отдельной старой логики
  - `_annotate_quality_payloads(...)` теперь определяет usable translation через:
    - `source_first_focus_text`
    - затем legacy unit/span fallback
  - `matched_by` / `alignment_kind` больше не считают legacy alignment основным источником там, где уже есть `source_first`
  - `_build_detail_unit_translation(...)` теперь сначала пытается брать:
    - единый unit-level focus
    - затем `source_first`
    - и только потом legacy target spans
  - это особенно поправило detail для time-block:
    - `7 00 AM -> 7 утра`

- обновлён [app/lib/src/detail_sheet_models.dart](/mnt/d/Programs/LEXO/app/lib/src/detail_sheet_models.dart)
  - local detail fallback теперь берёт:
    - `source_first.effective_coverage`
    - `source_first_focus_text`
    - и только потом legacy focus/span
  - переводы unit-ов в local detail path тоже подняты на `source-first` first

### Что проверено

- `python3 -m py_compile engine/storage.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite снова прошёл `PASS`
- ручная detail-проверка на `The Sunny Morning`:
  - `Good -> Доброе утро`
  - `Luna -> Луна`
  - `7 00 AM -> 7 утра`

### Что это дало

- legacy span/alignment больше не является основным semantic source в `detail` и quality-оценке, если уже есть usable `source-first`
- reader/detail/local fallback стали заметно ближе к режиму:
  - `source-first primary`
  - `legacy fallback`

## Сессия: Вырезаны `ANCHOR_TRANSLATIONS` и `REPORTING_VERB_TRANSLATIONS` из legacy alignment runtime

### Что сделано

- обновлён [engine/word_alignment.py](/mnt/d/Programs/LEXO/engine/word_alignment.py)
  - удалены runtime словари:
    - `ANCHOR_TRANSLATIONS`
    - `REPORTING_VERB_TRANSLATIONS`
  - `_anchor_candidates(...)` больше не расширяет hard-anchor кандидаты вручную через русские словарные соответствия
  - `_trim_pre_anchor_reporting_target_end(...)` больше не подрезает target span через reporting-verb словарь
  - `_find_predicate_target_index(...)` больше не исключает словарный набор reporting targets
  - `_rescue_reporting_verb_targets(...)` и `_cleanup_reporting_tail_leaks(...)` отключены как словарно-зависимые rescue/cleanup ветки

### Что проверено

- `python3 -m py_compile engine/word_alignment.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite снова прошёл `PASS`

### Что это дало

- legacy alignment больше не принимает runtime решения через ручные `anchor/reporting` словари
- `source-first` path теперь отделён от старого словарного слоя не только концептуально, но и в коде

## Сессия: Вычищен curated lexical layer из `lexical_enrichment` и `v2 lookup`

### Что сделано

- обновлён [engine/lexical_enrichment.py](/mnt/d/Programs/LEXO/engine/lexical_enrichment.py)
  - удалён `DIRECT_MEANINGS`
  - удалён словарный `GRAMMAR_HINTS`
  - `direct_meaning_for_word(...)` больше не возвращает ручной мини-перевод
  - `grammar_hint_for_word(...)` переведён на rule-based генерацию от `pos` и типа служебного слова
  - отдельные формулировки для `the` и `a/an` сохранены как rule-ветки, а не как runtime lexical dictionary

- обновлён [engine/v2_core/lookup.py](/mnt/d/Programs/LEXO/engine/v2_core/lookup.py)
  - function units больше не получают `base_translation` из manual direct meaning
  - lexical units больше не получают `base_translation` из manual direct meaning
  - `v2 lookup` теперь строит только structural explanation / fallback explanation, без словарного перевода как source of truth

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - исправлен structural fallback для object/noun groups без subject coverage
  - кейс `He drinks orange juice. -> Он пьет апельсиновый сок.` снова даёт `orange -> апельсиновый сок`, а не `пьет апельсиновый сок`

### Что проверено

- `python3 -m py_compile engine/lexical_enrichment.py engine/v2_core/lookup.py engine/v2_core/coverage.py engine/storage.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite снова прошёл `PASS`

### Что это дало

- в runtime больше не осталось ручного lexical translation слоя для `source-first` / detail / lookup
- `grammar_hint` остался только как explain-only слой
- primary semantic path теперь не опирается на:
  - `ANCHOR_TRANSLATIONS`
  - `REPORTING_VERB_TRANSLATIONS`
  - `DIRECT_MEANINGS`

## Сессия: `source_analyzer` переведён на parser-first/degraded вместо verb-list эвристик

### Что сделано

- обновлён [engine/lexical_enrichment.py](/mnt/d/Programs/LEXO/engine/lexical_enrichment.py)
  - удалены heuristic semantic lists:
    - `IRREGULAR_LEMMAS`
    - `MODAL_WORDS`
    - `AUXILIARY_WORDS`
    - `PRONOUN_WORDS`
    - `SUBORDINATOR_WORDS`
    - `PARTICLE_WORDS`
    - `VERB_SUFFIXES`
  - `_heuristic_pos(...)`, `_heuristic_lemma(...)`, phrase detection и context overrides больше не используются как primary fallback
  - если spaCy/parser недоступен, слова получают:
    - `pos = X`
    - `parser_status = degraded_no_parser`
    - без `dep/head_index`
  - оставлен только минимальный degraded fallback для:
    - articles
    - numbers
    - punctuation

- обновлён [engine/v2_core/models.py](/mnt/d/Programs/LEXO/engine/v2_core/models.py)
  - `SourceToken` расширен полями:
    - `dep`
    - `head_index`
    - `parser_status`

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - удалена primary логика `_is_predicate_token(...)` с hardcoded verb lemmas
  - relations теперь строятся только из dependency parse:
    - `nsubj/nsubjpass/expl -> subject_of`
    - `obj/dobj/iobj/pobj/attr/oprd/dative -> object_of`
    - `amod/compound/poss/nummod -> modifier_of`
    - `prt -> particle_of`
    - `conj -> coord_with`
  - если dependency parse отсутствует, graph остаётся degraded:
    - без уверенных relations
    - без groups, которые раньше строились по спискам

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - regression contract теперь различает:
    - parser available: требуются dependency relations/groups
    - parser missing: требуется честный degraded graph без ложных groups/focus

### Что проверено

- `python3 -m py_compile engine/lexical_enrichment.py engine/v2_core/models.py engine/v2_core/source_analyzer.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite снова прошёл `PASS`
- отдельная проверка окружения:
  - `spacy_pkg False`

### Что это дало

- `source-first` больше не угадывает verbs/nouns/adjectives через бесконечные списки
- без parser-а система теперь не врёт: отдаёт degraded graph вместо fake relations
- следующий обязательный практический шаг:
  - установить English parser/tagger (`spaCy` + `en_core_web_sm` или другой offline parser)
  - после этого пересобрать `source-first` contour для книг

## Сессия: Установлены offline spaCy parser models в проектный Windows venv

### Что сделано

- в проектный Windows venv [/.venv](/mnt/d/Programs/LEXO/.venv) установлены:
  - `spacy 3.8.14`
  - `en_core_web_sm 3.8.0`
  - `ru_core_news_sm 3.8.0`
- `ru_core_news_sm` дополнительно подтянул/использует:
  - `pymorphy3`
  - `pymorphy3-dicts-ru`

### Что проверено

- подтверждено наличие пакетов в `.venv/Lib/site-packages`:
  - `spacy`
  - `en_core_web_sm`
  - `ru_core_news_sm`
  - `pymorphy3`
- прямой smoke-запуск Windows Python из текущей WSL-сессии не прошёл из-за окружения:
  - `WSL ERROR: UtilBindVsockAnyPort:307: socket failed 1`

### Что это даёт

- Windows launcher, который запускает backend через `.venv/Scripts/python.exe`, теперь должен видеть English parser
- `source-first` в реальном Windows runtime сможет выйти из `degraded_no_parser` и строить relations/groups через dependency parse
- Russian parser установлен как база для следующего шага:
  - target coverage validation на русском
  - RU token role / morphology validation

## Сессия: Анализ `The Sunny Morning` после подключения English parser

### Что проверено

- активная книга:
  - `book_f9ab0aa3a6c9`
  - `The Sunny Morning`
  - модель перевода: `Helsinki-NLP/opus-mt-en-ru`
- в persisted `source_analysis_json`:
  - `36` сегментов
  - `244` source tokens
  - все `244` токена имеют `parser_status = parsed`
  - `degraded_no_parser = 0`

### Агрегаты source-first

- segment types:
  - `simple_action`: `27`
  - `heading_title`: `1`
  - `heading_chapter`: `4`
  - `time_phrase`: `2`
  - `quote_dialogue`: `1`
  - `post_quote_clause`: `1`
- unit types:
  - `FUNCTION`: `66`
  - `LEXICAL`: `111`
  - `META`: `67`
- relations:
  - `subject_of`: `31`
  - `object_of`: `23`
  - `modifier_of`: `23`
  - `coord_with`: `7`
  - `particle_of`: `1`
- groups:
  - `noun_phrase`: `23`
  - `coordinated_objects`: `7`
  - `phrasal_verb`: `1`

### Что работает хорошо

- English parser реально заменил verb-list угадывание:
  - `wakes -> VERB/root`
  - `orange/juice` разбираются через modifier/object graph
  - `eggs and toast` строит coordinated group
  - `wakes up` строит phrasal group через `prt`
- удачные source-first cases:
  - `wakes up -> просыпается`
  - `7:00 AM -> 7 утра`
  - `eggs and toast -> яйца и тосты`
  - `orange juice -> апельсиновый сок`
  - `her cat food -> свою кошачью пищу`
  - `big dog -> большую собаку`
  - `good day -> хороший день`

### Основные проблемы

- target coverage всё ещё эвристический и не использует `ru_core_news_sm`
- из-за этого group coverage часто слишком широкий:
  - `New Day -> 1 : Новый день`
  - `goes to the kitchen and sees -> кухню и видит своего кота , Луну`
  - `his cat -> идет на кухню и видит своего кота , Луну`
  - `Good morning -> утро , Луна`
  - `big garden -> большой сад с красными цветами и зелеными деревьями`
  - `beautiful day -> день " , думает Том`
  - `his friend -> он видит свою подругу Анну`
- parser correctly emits some relations, but current group builder interprets all `coord_with` too broadly:
  - verb coordination like `goes ... and sees` is incorrectly materialized as `coordinated_objects`
  - adjective coordination like `small and grey` currently covers only `серая`, missing `маленькая`
- tokenizer/source graph has a weak spot on hyphenated words:
  - `T-shirt` produces bad group candidates around `blue T-shirt and white shoes`

### Вывод

- English parser подключился и работает: система больше не строит source graph по verb-list эвристикам
- следующий bottleneck теперь target-side:
  - подключить `ru_core_news_sm` к coverage validation
  - сузить group coverage по RU POS/dep/morph
  - ограничить group builder: не превращать любую `conj` relation в `coordinated_objects`

## Сессия: Начато подключение RU parser к coverage и ограничение coordination groups

### Что сделано

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - `coord_with` больше не превращается автоматически в `coordinated_objects`
  - predicate coordination (`VERB/AUX`, `root`) не материализуется как object group
  - grouping coordination оставлен только для nominal/adjective-like units

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - добавлен lazy-loader для `ru_core_news_sm`
  - `target_tokens` расширены RU parser metadata:
    - `lemma`
    - `pos`
    - `dep`
    - `head_index`
    - `parser_status`
  - для `noun_phrase` и `coordinated_objects` добавлено первичное сужение coverage window по RU nominal/coord spans

- обновлён [engine/qa_ru_quality.py](/mnt/d/Programs/LEXO/engine/qa_ru_quality.py)
  - исправлен WSL/runtime конфликт: Windows `.venv/Lib/site-packages` теперь добавляется временно только для `pymorphy3`, а затем удаляется из `sys.path`
  - это предотвращает импорт Windows `spacy/numpy` wheels внутри WSL Python

- обновлён [engine/storage.py](/mnt/d/Programs/LEXO/engine/storage.py)
  - `rebuild_book_source_first(...)` теперь защищён от перезаписи parsed contour degraded-анализом
  - если в БД уже есть `parser_status=parsed`, а новый анализ без parser-а, сегмент пропускается и считается в `skipped_degraded_overwrite`

### Что проверено

- `python3 -m py_compile engine/storage.py engine/qa_ru_quality.py engine/v2_core/source_analyzer.py engine/v2_core/coverage.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite снова прошёл `PASS`

### Важное ограничение текущей WSL-сессии

- Windows spaCy models установлены в `.venv`, но WSL не может запускать Windows Python:
  - `WSL ERROR: UtilBindVsockAnyPort:307: socket failed 1`
- WSL Python не имеет собственного `pip/spaCy`
- попытка `rebuild_book_source_first(...)` из WSL перезаписала `The Sunny Morning` degraded-contour до добавления guard
- для финального анализа RU parser coverage нужно:
  - запустить backend из Windows launcher
  - пересобрать `source-first` contour книги из Windows runtime
  - затем повторить отчёт

## Сессия: Полный повторный тест `The Sunny Morning` после загрузки новой книги

### Что проверено

- активная книга:
  - `book_a648f0f3a090`
  - `The Sunny Morning`
  - `36` сегментов
- `python3 scripts/check_pipeline_regressions.py`
  - полный regression suite прошёл `PASS`
- `scripts/analyze_translation_segmentation.py` прогнан по всем `8` параграфам активной книги
- дополнительно сняты агрегаты persisted `source_analysis_json` / `source_coverage_json`

### Агрегаты

- source parser:
  - `parsed`: `244`
- target parser:
  - `parsed`: `214`
- relations:
  - `modifier_of`: `23`
  - `object_of`: `23`
  - `subject_of`: `31`
  - `coord_with`: `7`
  - `particle_of`: `1`
- groups:
  - `noun_phrase`: `23`
  - `coordinated_objects`: `6`
  - `phrasal_verb`: `1`
- group coverage:
  - `exact`: `26`
  - `reordered`: `4`

### Что работает

- English и Russian spaCy parser реально подключены в persisted contour новой книги
- degraded overwrite не наблюдается
- базовые source-first кейсы работают:
  - `wakes up -> просыпается`
  - `7:00 AM -> 7 утра`
  - `eggs and toast -> яйца и тосты`
  - `orange juice -> апельсиновый сок`
  - `big dog -> большую собаку`
  - `good day -> хороший день`

### Что ломается

- group coverage всё ещё может привязывать группу к неверному русскому span:
  - `his cat -> кухню`
  - `red flowers -> большой сад`
  - `green trees -> большой сад`
- coordination builder всё ещё слишком грубо называет adjective coordination как `coordinated_objects`:
  - `small and grey -> серая`
  - `brown and very friendly -> дружелюбная`
  - `tired but very happy -> счастлив`
- hyphenated token case `T-shirt` ломает source graph:
  - parser раскладывает `T-shirt` как `T - shirt`
  - система строит кривую группу `shirt and white`
- некоторые heading/meta units получают group-level coverage не полностью:
  - `Chapter 1 -> Глава`
  - число остаётся отдельным token-level alignment, но group span неполный

### Вывод

- текущий bottleneck уже не в словарях и не в отсутствии parser models
- ломается слой построения групп и target-side coverage narrowing
- следующий технический шаг:
  - разделить `coordinated_objects` и adjective/property coordination
  - запретить modifier group coverage цепляться за unrelated previous nominal span
  - добавить нормализацию hyphenated source tokens до построения graph/groups

## Сессия: Первый fail-closed фикс source-first group coverage

### Что сделано

- обновлён [engine/v2_core/models.py](/mnt/d/Programs/LEXO/engine/v2_core/models.py)
  - добавлен новый тип группы:
    - `coordinated_properties`

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - adjective coordination больше не маркируется как `coordinated_objects`
  - `ADJ + ADJ` теперь даёт `coordinated_properties`
  - смешанная coordination вроде `NOUN + ADJ` больше не материализуется как группа
  - это защищает кейс `T-shirt` от кривой группы `shirt and white`

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - добавлен fail-closed guard для structural fallback
  - если noun phrase имеет конкурирующие object-кандидаты раньше по source graph, система больше не отдаёт уверенный group `exact`
  - `coordinated_properties` пока не получает object fallback, чтобы не показывать ложный exact по одному последнему прилагательному

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - добавлен regression-блок `source_first_fail_closed_group_coverage`
  - зафиксированы классы ошибок:
    - `his cat` не должен цепляться к `кухню`
    - `red flowers` / `green trees` не должны цепляться к `большой сад`
    - `small and grey` не должен быть `coordinated_objects`
    - `T-shirt` не должен порождать группу `shirt and white`

### Что проверено

- `python3 -m py_compile engine/v2_core/models.py engine/v2_core/source_analyzer.py engine/v2_core/coverage.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- отдельный parser-independent smoke показал:
  - `NOUN + ADJ` не группируется
  - `ADJ + ADJ` становится `coordinated_properties`
  - noun phrase с конкурирующим object-кандидатом не получает structural fallback

### Ограничение проверки

- текущий WSL Python не видит spaCy models:
  - `en_parser False`
  - `ru_parser False`
- Windows `.venv` из WSL по-прежнему не запускается:
  - `WSL ERROR: UtilBindVsockAnyPort:307: socket failed 1`
- поэтому активную книгу нужно пересобрать через Windows launcher/backend, а не из WSL

## Сессия: Уточнение group coverage: partial exact guard и RU phrase expansion

### Что сделано

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - group coverage теперь проверяет не только число source members с coverage, но и число русских content tokens в target window
  - если несколько английских смысловых units схлопнулись в один русский token, группа больше не получает `exact` автоматически
  - для `noun_phrase` добавлено расширение частичного окна по соседним RU nominal tokens:
    - `утро` может расширяться до `Доброе утро`
    - `день` может расширяться до `прекрасный день`
  - для групп после source delimiter `:` target search теперь начинается после target `:`
  - `modifier_head` fallback уточнён:
    - разрешён для non-object/title/formula-like фраз без object-конкурентов
    - запрещён внутри сложных object/preposition цепочек, где легко прыгнуть на предыдущую noun phrase

### Что проверено

- `python3 -m py_compile engine/v2_core/coverage.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- диагностический пересчёт group coverage поверх сохранённых parsed target tokens активной книги без записи в БД показал:
  - `Sunny Morning -> Солнечное утро`
  - `New Day -> Новый день`
  - `Good morning -> Доброе утро`
  - `beautiful day -> прекрасный день`
  - `green trees -> none` вместо ложного `большой сад`
  - `T-shirt -> none` вместо ложного/слишком широкого `синюю футболку`

### Что остаётся

- отдельное word-level разделение внутри group ещё не доведено:
  - `Good -> Доброе`
  - `morning -> утро`
  - `beautiful -> прекрасный`
  - `day -> день`
- `T-shirt` всё ещё требует отдельной нормализации hyphenated source tokens до построения graph
- possessive связи (`his/her`) ещё нужно выделить как отдельный structural layer, чтобы не терять `его/свою`

## Сессия: Word-level split внутри exact noun phrase groups

### Что сделано

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - после вычисления exact `noun_phrase` group coverage система теперь пытается разложить group span обратно на отдельные source units
  - split разрешён только если:
    - группа имеет `coverage_status = exact`
    - source units являются смысловыми (`LEXICAL` или non-punctuation `META`)
    - число source units равно числу RU content tokens внутри group span
  - split не использует словари и работает по структурному порядку внутри уже подтверждённой группы

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - добавлен parser-independent regression `source_first_group_split_to_units`
  - тест фиксирует:
    - `Good -> Доброе`
    - `morning -> утро`

### Что проверено

- `python3 -m py_compile engine/v2_core/coverage.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- диагностический пересчёт поверх сохранённых parsed данных активной книги показал:
  - `Sunny -> Солнечное`
  - `Morning -> утро`
  - `New -> Новый`
  - `Day -> день`
  - `Good -> Доброе`
  - `morning -> утро`
  - `beautiful -> прекрасный`
  - `day -> день`

### Что остаётся

- чтобы это появилось в persisted runtime payload книги, нужно пересобрать `source-first` contour через Windows backend/launcher, где доступны spaCy models
- следующий отдельный слой:
  - нормализация hyphenated source tokens (`T-shirt`)
  - possessive split для `his/her -> его/свою`

## Сессия: Hyphenated source tokens и possessive split

### Что сделано

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - source tokenizer теперь сохраняет hyphenated слова как один token:
    - `T-shirt`
    - `well-known`
    - `ice-cream`
  - это убирает искусственные source units вида `T`, `-`, `shirt`
  - правило структурное и не содержит book-specific фраз

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - RU `DET` добавлен в content/nominal POS для noun phrase coverage
  - possessive source units с `dep=poss` теперь могут получать отдельный target token внутри exact noun phrase group
  - target possessive выбирается структурно:
    - RU token `pos=DET/PRON`
    - dependency `det/nmod/poss`
    - token находится внутри той же noun phrase target window
  - словарь `his/her -> его/свою` не добавлялся

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - добавлен regression `hyphenated_source_token_and_possessive_split`
  - тест фиксирует:
    - `T-shirt` сохраняется одним source token
    - `T` и `shirt` не появляются как отдельные source tokens
    - `his legs -> его ногах` раскладывается:
      - `his -> его`
      - `legs -> ногах`

### Что проверено

- `python3 -m py_compile engine/v2_core/source_analyzer.py engine/v2_core/coverage.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- диагностический пересчёт поверх сохранённых parsed data активной книги показал:
  - `his legs -> его ногах`
  - `his -> его`
  - `legs -> ногах`
  - `her cat food -> свою кошачью пищу`
  - `her -> свою`
  - `his cat` и `his friend` остаются `none`, потому что там fail-closed guard не даёт прыгнуть на чужой span

### Что остаётся

- для `T-shirt` эффект в активной книге появится только после пересборки source-first contour через Windows backend/launcher
- для сложных групп вроде `her cat food` ещё можно улучшать вложенный split:
  - `cat food -> кошачью пищу`
  - `cat -> кошачью`
  - `food -> пищу`

## Сессия: Полный source-first отчёт по активной `The Sunny Morning`

### Что проверено

- активная книга:
  - `book_21e79f34ab2c`
  - `The Sunny Morning`
  - `36` segments
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- создан полный QA-отчёт:
  - [txt/qa/2026-04-23_1406_sunny_morning_source_first_full_report.md](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1406_sunny_morning_source_first_full_report.md)

### Агрегаты отчёта

- source parser:
  - `parsed`: `233`
  - `degraded_no_parser`: `9`
- target parser:
  - `parsed`: `214`
- relations:
  - `modifier_of`: `21`
  - `object_of`: `22`
  - `subject_of`: `30`
  - `coord_with`: `6`
  - `particle_of`: `1`
- groups:
  - `noun_phrase`: `21`
  - `coordinated_properties`: `3`
  - `coordinated_objects`: `2`
  - `phrasal_verb`: `1`
- group coverage:
  - `exact`: `19`
  - `reordered`: `1`
  - `none`: `7`

### Что ломается

- сегмент `He wears a blue T-shirt and white shoes.` полностью ушёл в `degraded_no_parser`
- причина:
  - source tokenizer теперь держит `T-shirt` как один token
  - spaCy внутри всё ещё разбивает это на `T`, `-`, `shirt`
  - `_annotate_with_spacy(...)` требует равное число tokens и при mismatch деградирует весь segment
- это нужно чинить отдельным mapper-слоем tokenizer ↔ parser, а не откатом hyphenated rule

### Что подтвердилось

- RU target parser работает на всех target tokens
- `possessive` split работает на сохранённых parsed data:
  - `his legs -> его ногах`
  - `his -> его`
  - `legs -> ногах`
  - `her cat food -> свою кошачью пищу`
  - `her -> свою`
- fail-closed guard оставляет `his cat` и `his friend` как `none`, чтобы не привязать их к чужим spans

## Сессия: Добавлен structural aligner для parser-role word coverage

### Что сделано

- добавлен [engine/v2_core/structural_aligner.py](/mnt/d/Programs/LEXO/engine/v2_core/structural_aligner.py)
  - новый слой после group coverage и word split
  - не содержит EN/RU словарей перевода
  - выравнивает по parser roles, POS, order и локальным phrase windows:
    - `PROPN -> RU PROPN`
    - `VERB -> RU VERB/AUX`
    - `coordinated_properties -> RU ADJ/VERB`
    - uncovered `noun_phrase -> RU nominal span`
    - possessive внутри noun phrase через RU `DET/PRON`

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - `V2CoverageResolver.resolve_analysis(...)` теперь применяет `V2StructuralAligner.refine(...)` перед сохранением `source_coverage_json`

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - добавлен regression `structural_aligner_parser_role_mapping`
  - тест фиксирует:
    - `Tom -> Том`
    - `goes -> идёт`

### Что проверено

- `python3 -m py_compile engine/v2_core/structural_aligner.py engine/v2_core/coverage.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- создан диагностический отчёт:
  - [txt/qa/2026-04-23_1419_sunny_morning_structural_aligner_report.md](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1419_sunny_morning_structural_aligner_report.md)

### Результат на активной книге в диагностическом пересчёте

- content unit coverage:
  - `exact`: `82`
  - `fuzzy`: `41`
  - `none`: `1`
- issues:
  - было `100` в предыдущем отчёте
  - стало `43`
- новые успешные alignments:
  - `goes -> идет/идёт`
  - `sees -> видит`
  - `small -> маленькая`
  - `grey -> серая`
  - `red -> красными`
  - `flowers -> цветами`
  - `green -> зелеными`
  - `trees -> деревьями`
  - `his cat -> своего кота`
  - `his friend -> свою подругу`
  - `brown -> коричневая`
  - `friendly -> дружелюбная`
  - `tired -> устал`
  - `happy -> счастлив`

### Что остаётся

- `T-shirt` сегмент всё ещё degraded до tokenizer/parser mapper fix
- часть одиночных nouns/adverbs остаётся fuzzy:
  - `home`
  - `book`
  - `sofa`
  - `park`
  - `breakfast`
  - `sun`
  - `bright`
  - `cat`
  - `Meow`
- это следующий слой: non-group lexical role alignment и tokenizer/parser alignment для hyphenated tokens

## Сессия: Tokenizer mapper и guarded non-group lexical alignment

### Что сделано

- обновлён [engine/lexical_enrichment.py](/mnt/d/Programs/LEXO/engine/lexical_enrichment.py)
  - добавлен mapper между project tokenizer и spaCy tokens
  - если project token вроде `T-shirt` spaCy разбивает на `T`, `-`, `shirt`, annotation больше не должна падать в degraded
  - head/POS/dep берутся с внутреннего head token, а source unit остаётся единым `T-shirt`

- обновлён [engine/v2_core/structural_aligner.py](/mnt/d/Programs/LEXO/engine/v2_core/structural_aligner.py)
  - добавлен guarded fallback для оставшихся одиночных lexical units:
    - `NOUN -> RU NOUN/PROPN/ADV`
    - `ADJ -> RU ADJ/VERB`
    - `ADV -> RU ADV/ADJ`
  - fallback ищет только после ближайшего уже выровненного предыдущего source unit
  - `PROPN` исключён из одиночного fallback, чтобы не получать ложные `Goodnight -> Luna`
  - global name alignment теперь работает только если число source/target names совпадает
  - для вложенных noun phrase с лишним possessive target span добавлен right-tail split:
    - `cat food -> кошачью пищу`
    - `cat -> кошачью`
    - `food -> пищу`

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - hyphenated regression теперь проверяет parsed `T-shirt` там, где доступен parser
  - structural aligner regression дополнен одиночным noun alignment:
    - `book -> книгу`

### Что проверено

- `python3 -m py_compile engine/lexical_enrichment.py engine/v2_core/structural_aligner.py engine/v2_core/coverage.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - полный suite прошёл `PASS`
- создан финальный диагностический отчёт:
  - [txt/qa/2026-04-23_1427_sunny_morning_structural_aligner_report_v2.md](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1427_sunny_morning_structural_aligner_report_v2.md)

### Результат в диагностическом пересчёте

- content unit coverage:
  - `exact`: `105`
  - `fuzzy`: `18`
  - `none`: `1`
- issues:
  - было `43`
  - стало `20`
- исправлены ложные привязки:
  - `Goodnight -> Luna` больше не делается
  - `Luna -> Tom` в goodnight-сегменте больше не делается
  - `cat food` теперь даёт:
    - `cat -> кошачью`
    - `food -> пищу`

### Что остаётся

- persisted active book всё ещё содержит degraded `T-shirt` segment до пересборки через Windows backend/launcher
- остаточные fuzzy:
  - `little`
  - `Goodnight/Luna/Tom/whispers` в goodnight dialogue
  - `Hello`
  - `am/says` в `I am great...`
  - `Meow`
  - несколько `Luna` cases, где безопаснее оставить fuzzy без entity/coreference слоя

## Сессия: Полный тест свежезалитой активной книги

### Что проверено

- активная книга в базе:
  - `book_95b49fa94631`
  - `Max’s New Day`
  - `22` сегмента
- свежий `The Sunny Morning` тоже есть в базе:
  - `book_0dc913c210d4`
  - но он не активный
- regression suite:
  - `python3 scripts/check_pipeline_regressions.py`
  - результат: `PASS`
- диагностический отчёт:
  - [txt/qa/2026-04-23_1435_Maxs_New_Day_active_book_mismatches.md](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1435_Maxs_New_Day_active_book_mismatches.md)

### Результат

- source parser:
  - `degraded_no_parser`: `206`
  - `parsed`: `3`
- target parser:
  - `parsed`: `173`
- groups:
  - `{}` — source groups не построились
- group coverage:
  - `{}` — нечего покрывать, потому что source side degraded
- content unit coverage:
  - `fuzzy`: `160`
  - `exact`: `1`
- всего issues:
  - `181`
- ключевые классы:
  - `word_no_target`: `160`
  - `parser_degraded`: `21`

### Вывод

- текущая свежезалитая активная книга импортирована/пересчитана почти полностью без English parser
- RU parser работает, target side parsed
- source-first segmentation/word-to-word alignment не может нормально работать без EN POS/dependency parse
- следующий шаг: проверить runtime backend, которым была залита книга, и добиться, чтобы upload/rebuild запускался в окружении с `en_core_web_sm`

## Сессия: Проверка причины деградации новой книги

### Что проверено

- текущий shell runtime:
  - `/usr/bin/python3`
  - `spacy`: не установлен
  - `en_core_web_sm`: не найден
  - `ru_core_news_sm`: не найден
- `V2SourceAnalyzer` в текущем runtime на `Max has a dog.` отдаёт:
  - `parser_status=degraded_no_parser`
  - `pos=X`
  - groups: `[]`
- Windows `.venv` из WSL сейчас не запускается:
  - `WSL ERROR: UtilBindVsockAnyPort:307: socket failed 1`
- backend на `127.0.0.1:8765` сейчас не отвечает
- активная книга:
  - `book_95b49fa94631`
  - `Max’s New Day`

### Что найдено в сохранённой активной книге

- `22` сегмента
- `21` сегмент сохранён с `degraded_no_parser`
- только один короткий сегмент parsed:
  - `In the evening`
- всего source tokens:
  - `degraded_no_parser`: `206`
  - `parsed`: `3`
- source groups:
  - `0`

### Вывод

- новая книга посыпалась не из-за новых слов, не из-за словарей и не из-за token mismatch
- причина: импорт/пересчёт прошёл в runtime без English spaCy parser
- симптом в данных прямой:
  - почти все слова имеют `pos=X`
  - `dep` пустой
  - `parser_status=degraded_no_parser`
- пока upload/rebuild запускается в таком runtime, source-first alignment будет давать массовый `fuzzy`

## Сессия: Повторная загрузка `Max’s New Day` и проверка parser runtime

### Что проверено

- пользователь удалил `book_95b49fa94631` и повторно импортировал `Max’s New Day`
- новая активная книга:
  - `book_f2b347d37c1d`
  - `Max’s New Day`
- сохранённый результат нового импорта:
  - segments: `22`
  - source parser `degraded_no_parser`: `206`
  - source parser `parsed`: `3`
  - parsed segment только один:
    - `In the evening`
  - source groups: `0`
- `.venv/Lib/site-packages` физически содержит:
  - `spacy`
  - `en_core_web_sm`
  - `ru_core_news_sm`
- создан отчёт:
  - [txt/qa/2026-04-23_1446_max_new_day_reimport_segmentation.txt](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1446_max_new_day_reimport_segmentation.txt)

### Вывод

- повторная загрузка не исправила проблему
- это не разовый сбой книги
- причина локализована до runtime загрузки parser:
  - либо engine запущен не через проектный `.venv`
  - либо `spacy.load("en_core_web_sm")` падает внутри Windows engine процесса, а текущий код молча возвращает `None`
- следующий практический шаг:
  - добавить явный startup/runtime diagnostic для parser-а и перестать молча скрывать причину `OSError/ImportError`

## Сессия: Startup diagnostics для parser runtime

### Что сделано

- добавлен [engine/parser_diagnostics.py](/mnt/d/Programs/LEXO/engine/parser_diagnostics.py)
  - печатает `sys.executable`
  - печатает `sys.version`
  - проверяет `importlib` spec для `spacy`
  - проверяет spec для `en_core_web_sm`
  - проверяет spec для `ru_core_news_sm`
  - пробует `spacy.load(...)` для обеих моделей
  - при ошибке печатает тип exception, текст exception и traceback
- обновлён [engine/main.py](/mnt/d/Programs/LEXO/engine/main.py)
  - диагностика вызывается перед стартом HTTP engine

### Что проверено

- `python3 -m py_compile engine/main.py engine/parser_diagnostics.py`
- `python3 scripts/check_pipeline_regressions.py`
  - результат: `PASS`

### Что смотреть в консоли при следующем запуске

- строки вида:
  - `[LEXO PARSER] python=...`
  - `[LEXO PARSER] en_core_web_sm=OK ...`
  - `[LEXO PARSER] ru_core_news_sm=OK ...`
- если модель не грузится:
  - `[LEXO PARSER] en_core_web_sm=ERROR ...`
  - ниже будет traceback с реальной причиной

## Сессия: Исправлен spaCy mapper для punctuation tokens

### Что найдено

- startup diagnostics показал:
  - Windows engine запущен из `.venv`
  - `en_core_web_sm=OK`
  - `ru_core_news_sm=OK`
- свежий импорт `The Sunny Morning` всё равно дал:
  - `parsed`: `9`
  - `degraded_no_parser`: `233`
  - parsed были только фразы без punctuation:
    - `The Sunny Morning`
    - `At 10:00 AM`
    - `In the afternoon`
- причина:
  - `_map_spacy_tokens_to_words(...)` не умел сопоставлять punctuation tokens
  - для `.`, `:`, `"`, `!` compact text пустой
  - mapper возвращал `None`
  - весь segment падал в degraded fallback
  - fallback ошибочно назывался `degraded_no_parser`, хотя parser был доступен

### Что сделано

- обновлён [engine/lexical_enrichment.py](/mnt/d/Programs/LEXO/engine/lexical_enrichment.py)
  - project punctuation token теперь сопоставляется с одним spaCy punctuation token
  - punctuation больше не валит весь segment в degraded
- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - добавлен regression `spacy_mapper_keeps_punctuation_tokens`

### Что проверено

- `python3 -m py_compile engine/lexical_enrichment.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - результат: `PASS`

### Следующий шаг

- перезапустить Windows engine
- заново импортировать книгу или добавить endpoint для `rebuild_book_source_first`
- после этого повторить mismatch report

## Сессия: Проверка нового импорта `Max’s New Day` после punctuation mapper fix

### Что проверено

- пользователь перезапустил Windows engine
- startup diagnostics:
  - `python=D:\Programs\LEXO\.venv\Scripts\python.exe`
  - `en_core_web_sm=OK`
  - `ru_core_news_sm=OK`
- новая активная книга:
  - `book_9166e6ad1744`
  - `Max’s New Day`
- создан отчёт:
  - [txt/qa/2026-04-23_1500_max_new_day_parser_fixed_segmentation.txt](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1500_max_new_day_parser_fixed_segmentation.txt)

### Результат

- segments: `22`
- source parser:
  - `parsed`: `209`
  - `degraded`: `0`
- source groups:
  - `27`
- source relations:
  - `91`
- group coverage:
  - `exact`: `22`
  - `reordered`: `2`
  - `none`: `3`
- content unit coverage:
  - `exact`: `96`
  - `fuzzy`: `13`

### Оставшиеся реальные косяки

- `gets up` ошибочно получает group coverage `утро`
- `seven o’clock` токенизируется плохо:
  - `seven o` как group
  - `clock` остаётся fuzzy
  - curly apostrophe `’` становится отдельным lexical token
- possessive/name construction:
  - `Its name ->` не покрывается в `Его зовут Рекс`
- clause noun phrase:
  - `his busy day`
  - `busy day`
  - не покрываются в `его трудный день`
- coordinated adjectives:
  - `friendly and happy -> дружелюбный счастливый`
  - статус `reordered`, потому что `и/очень` не входят в target group
- часть semantic/structural cases остаётся fuzzy:
  - `together`
  - existential `are`
  - possession `has`
  - some time phrase tails

### Вывод

- основной parser/mapping blocker исправлен
- теперь система реально работает parser-first без деградации
- следующие правки должны быть уже по логике:
  - нормализация `o’clock`
  - запрет ложного phrasal group coverage
  - possessive/name pattern через dependency parse
  - сужение/расширение coordinated property target spans

## Сессия: Правки time/phrasal/possessive/property coverage

### Что сделано

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - tokenizer теперь держит curly apostrophe внутри английского слова:
    - `o’clock` больше не режется как `o`, `’`, `clock`
  - `o'clock/o’clock` помечается как `META time`
  - `’` добавлен в punctuation markers

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - phrasal verb fallback теперь не может покрыться RU noun-only span
  - fallback ищет RU `VERB/AUX` в допустимом окне
  - кейс `gets up -> утро` должен исчезнуть

- обновлён [engine/v2_core/structural_aligner.py](/mnt/d/Programs/LEXO/engine/v2_core/structural_aligner.py)
  - добавлен structural pattern для possessive name:
    - `Its name` может покрываться `Его зовут`
    - `name -> зовут`
    - `Its -> Его`
  - добавлено completion для possessive noun phrase:
    - если content уже покрыт, group расширяется влево на RU possessive token
    - пример: `his busy day -> его трудный день`
  - coordinated properties теперь строят group span по всему RU диапазону, включая связку/усилитель:
    - пример: `friendly and happy -> очень дружелюбный и счастливый`

- обновлён [engine/api.py](/mnt/d/Programs/LEXO/engine/api.py)
  - добавлен endpoint:
    - `POST /books/rebuild-source-first`
  - теперь можно пересобрать source-first contour без удаления и повторного импорта книги

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - добавлен regression `time_and_structural_coverage_repairs`
  - проверяет:
    - `o’clock` token
    - phrasal fallback uses RU verb
    - possessive name pattern

### Что проверено

- `python3 -m py_compile engine/api.py engine/v2_core/source_analyzer.py engine/v2_core/coverage.py engine/v2_core/structural_aligner.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - результат: `PASS`

### Что нужно сделать в Windows engine

- перезапустить engine, чтобы подхватить код
- вызвать:
  - `POST /books/rebuild-source-first`
  - body: `{"book_id":"book_9166e6ad1744"}`
- затем повторить отчёт по активной книге

## Сессия: Анализ после rebuild и дополнительная коррекция ложного exact

### Что проверено после rebuild

- активная книга:
  - `book_3edbeace1f19`
  - `Max’s New Day`
- создан отчёт:
  - [txt/qa/2026-04-23_1515_active_after_rebuild_segmentation.txt](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1515_active_after_rebuild_segmentation.txt)
- агрегаты:
  - segments: `22`
  - parser `parsed`: `205`
  - degraded segments: `0`
  - groups: `27`
  - relations: `91`
  - group coverage:
    - `exact`: `23`
    - `reordered`: `1`
    - `none`: `3`
  - content unit coverage:
    - `exact`: `95`
    - `fuzzy`: `10`

### Что найдено

- `gets up` исправился:
  - было `утро`
  - стало `встаёт`
- `o’clock` исправился:
  - больше нет отдельного `o`, `’`, `clock`
- найден ложный `exact`:
  - `quiet mornings` мог покрываться как `его трудный день`
  - причина: RU nominal fallback выбирал самый длинный nominal span дальше по предложению, а не ближайший после predicate
- `Its name` ещё не покрылся:
  - RU parser помечает `Его` как `PRON obj`, а не possessive `det`

### Что дополнительно исправлено

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - `_best_ru_nominal_span(...)` теперь берёт ближайший nominal span, а не самый длинный
  - это должно убрать ложное `quiet mornings -> его трудный день`
- обновлён [engine/v2_core/structural_aligner.py](/mnt/d/Programs/LEXO/engine/v2_core/structural_aligner.py)
  - `Its name` pattern теперь принимает owner-pronoun перед `звать`, даже если RU dep=`obj`

### Что проверено

- `python3 -m py_compile engine/v2_core/coverage.py engine/v2_core/structural_aligner.py`
- `python3 scripts/check_pipeline_regressions.py`
  - результат: `PASS`

### Следующий шаг

- нужен ещё один Windows engine restart + `POST /books/rebuild-source-first`
- после этого список остаточных ошибок будет уже финальным для этого слоя

## Сессия: Полный аудит активной книги

### Что сделано

- проведён полный аудит всех `22` сегментов активной книги `Max’s New Day`
- отчёт:
  - [txt/qa/2026-04-23_1525_full_book_alignment_audit.txt](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1525_full_book_alignment_audit.txt)

### Агрегаты текущей базы

- parser:
  - `parsed`: `205`
- group coverage:
  - `exact`: `23`
  - `reordered`: `1`
  - `none`: `3`
- unit coverage:
  - `absorbed`: `70`
  - `exact`: `95`
  - `none`: `30`
  - `fuzzy`: `10`

### Найденные классы проблем

- segmentation/coverage:
  - `gets up -> встаёт` остаётся `reordered`, но уже не ложный `утро`
  - `his busy day`
  - `busy day`
  - `Its name`
  - `Max` в time-leading segments
  - `Rex` в `Rex is very friendly...`
  - `together`
  - existential `are`
  - possessive `has`
  - `bed` в `goes to bed`
- MT/RU quality:
  - `busy day -> трудный день`
  - `goes to university -> ходит в университет`
  - `маленький кафе`
  - `goes home -> уезжает домой`
  - `goes to bed -> ляжет спать`
- false-positive audit issue:
  - `their -> своей` помечен как function target, но это валидный possessive mapping и не должен считаться ошибкой

### Вывод

- предыдущие точечные проверки были недостаточны
- нужен отдельный регулярный full-book audit после каждого rebuild
- текущая система уже parser-first, но следующий слой должен закрывать structural possession/time-leading subject/ADV/existential patterns и отделять MT quality от segmentation

## Сессия: Полный тест свежезагруженной активной книги

### Что проверено

- активная книга:
  - `book_8efbb8c022bd`
  - `Max’s New Day`
  - created `2026-04-23T15:19:20.603413+00:00`
- отчёты:
  - [txt/qa/2026-04-23_1535_active_uploaded_segmentation_report.txt](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1535_active_uploaded_segmentation_report.txt)
  - [txt/qa/2026-04-23_1535_active_uploaded_full_audit.txt](/mnt/d/Programs/LEXO/txt/qa/2026-04-23_1535_active_uploaded_full_audit.txt)

### Агрегаты

- segments: `22`
- parser:
  - `parsed`: `205`
- group coverage:
  - `exact`: `26`
  - `reordered`: `1`
- unit coverage:
  - `exact`: `100`
  - `absorbed`: `68`
  - `none`: `30`
  - `fuzzy`: `7`
- issues:
  - `group_reordered`: `1`
  - `unit_fuzzy`: `7`
  - `suspicious_exact`: `5`
  - `function_target`: `3`
  - `mt_quality`: `5`

### Главные найденные несоответствия

- time-leading segment:
  - `Every morning, Max gets up at seven o’clock.`
  - `morning -> часов`
  - `seven -> утро`
  - `o’clock -> Макс`
  - `Max` остался fuzzy
  - причина: time phrase/group coverage перетягивает соседние target tokens
- time-leading bedtime segment:
  - `At eleven o’clock, Max goes to bed.`
  - `eleven -> часов`
  - `o’clock -> Макс`
  - `Max` fuzzy
  - `bed` fuzzy
  - причина: time expression и subject/object alignment конфликтуют с target order
- adverb:
  - `together` fuzzy
  - причина: ADV structural alignment не нашёл/не удержал `вместе`
- existential:
  - `are` fuzzy в `there are many...`
  - причина: existential construction переводится как `много`, а не обычный RU verb
- possession:
  - `has` fuzzy в `He has two best friends...`
  - причина: possession construction переводится как `У него ...`, без обычного глагола
- name/coreference:
  - `Rex` fuzzy в `Rex is very friendly...`
  - причина: proper noun duplicate/name guard или target already used
- function targets:
  - `his -> его`
  - `Its -> Его`
  - `their -> своей`
  - это не обязательно ошибка: possessive function words могут иметь явный target
- MT/RU quality:
  - `busy day -> трудный день`
  - `goes to university -> ходит в университет`
  - `маленький кафе`
  - `goes home -> уезжает домой`
  - `goes to bed -> ляжет спать`

### Вывод

- parser и базовый source-first контур работают
- главные реальные проблемы сейчас:
  - time expression coverage
  - structural ADV alignment
  - existential/possession constructions
  - proper noun duplicate guard
  - отделение MT quality от coverage ошибок

## Сессия: Fail-closed coverage для time phrase и group split

### Что сделано

- обновлён [engine/v2_core/models.py](/mnt/d/Programs/LEXO/engine/v2_core/models.py)
  - добавлен `GroupType.TIME_PHRASE`

- обновлён [engine/v2_core/source_analyzer.py](/mnt/d/Programs/LEXO/engine/v2_core/source_analyzer.py)
  - modifier group с time meta-unit теперь строится как `time_phrase`, а не `noun_phrase`
  - пример:
    - `seven o’clock`
    - `eleven o’clock`

- обновлён [engine/v2_core/coverage.py](/mnt/d/Programs/LEXO/engine/v2_core/coverage.py)
  - добавлен отдельный matcher для RU time span:
    - `NUM + час/утра/вечера/...`
  - `time_phrase` больше не проходит через generic nominal fallback
  - split time phrase теперь раздаёт:
    - `NUM -> RU NUM`
    - `o’clock -> RU time word`
  - добавлен fail-closed gate для generic group split:
    - `NUM` не может получить RU `NOUN`
    - `PROPN` не может получить не-`PROPN`
    - `META time` не может получить person/name token
    - `ADJ/ADV/NOUN` ограничены совместимыми target POS

- обновлён [scripts/check_pipeline_regressions.py](/mnt/d/Programs/LEXO/scripts/check_pipeline_regressions.py)
  - расширен regression `time_and_structural_coverage_repairs`
  - проверяет:
    - `seven o’clock -> семь часов`
    - `seven -> семь`
    - `o’clock -> часов`

### Что проверено

- `python3 -m py_compile engine/v2_core/models.py engine/v2_core/source_analyzer.py engine/v2_core/coverage.py scripts/check_pipeline_regressions.py`
- `python3 scripts/check_pipeline_regressions.py`
  - результат: `PASS`

### Ожидаемый эффект после Windows rebuild

- должны уйти ложные пары:
  - `seven -> утро`
  - `o’clock -> Макс`
  - `eleven -> часов` частично: `eleven` должен идти в `11`, `o’clock` в `часов`
- если система не может безопасно сопоставить, она должна оставить `fuzzy`, а не ставить ложный `exact`

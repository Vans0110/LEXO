# 2026-06-12

## Упрощение интерфейса Workbench

- Из нижней части Workbench удалён устаревший ручной редактор книги:
  `Load TXT`, import/generate/export/update, поля title/cover/level/section/chapter.
- `Clean selected` и `Sync to R2` перенесены в верхнюю панель действий
  `Book Library` над карточками книг.
- `Clean selected` доступен только при выбранных книгах; `Sync to R2` —
  когда Workbench не занят.
- Внутренний пакетный pipeline импорта, генерации и экспорта сохранён.
- Удалены неиспользуемые ручные entry-point методы, picker-импорты,
  controller и отдельный polling одиночной генерации.
- `dart analyze lib/src/workbench`: без замечаний; тесты фильтров: 4/4.

## Применение фильтров Workbench

- В фильтры Book Library добавлена кнопка `Apply`.
- Значения в выпадающих списках теперь являются черновыми и не меняют
  список книг до явного применения.
- Применённые level/chapter/build-status сохраняются во время обработки и
  последующих обновлений статусов.
- При refresh Workbench показывает последний загруженный отфильтрованный
  список и линейный индикатор вместо временной замены всей библиотеки.
- `Process all` по-прежнему обрабатывает всю библиотеку; фильтр влияет на
  отображение и действия с видимыми книгами.
- Добавлен widget-тест кнопки `Apply`; тесты фильтров и `dart analyze`
  проходят без замечаний.

## Адаптивная сетка книг главы

- На экране книг главы сохранён существующий размер карточки и обложки.
- Простой `Wrap` заменён адаптивной сеткой: две колонки на узком экране и
  три, когда помещаются три карточки с минимальными промежутками.
- В каждой строке свободное место равномерно распределяется слева, справа и
  между карточками; неполная последняя строка также центрируется.
- `dart analyze` изменённого экрана: без замечаний.

## Числа в учебных текстах

- Для всего корпуса A1-C1 закреплено правило: в читательском тексте числа,
  даты, время, цены, адреса, номера помещений и транспорта, размеры,
  расстояния и температуры записываются английскими словами.
- Единственное исключение зарезервировано для полноценного телефонного
  номера, который может храниться цифрами как отдельный тип данных.
- Обновлены общий промпт создания книги, производственный процесс A1-C1,
  контракты A1/A2 и обязательная самопроверка перед сохранением текста.
- Во всех 100 книгах A1 и пяти существующих книгах A2 цифры контекстно
  преобразованы в слова; простые механические замены не использовались.
- Повторно экспортированы 68 рабочих A1 TXT и синхронизированы backend-копии;
  также исправлено время в двух дополнительных демонстрационных книгах.
- Аудиты A1/A2 теперь отклоняют любую цифру вне распознанного телефонного
  номера.
- Проверки: цифр нет в телах A1/A2, 68 рабочих TXT и 24 backend-книгах;
  структурные аудиты A1/A2 и строгий лексический аудит A1 успешны.

## Формат абзацев учебного корпуса

- Удалены ручные переносы по ширине строки внутри абзацев во всех 100
  исходных историях A1 и пяти существующих историях A2.
- Из исправленных A1-исходников повторно экспортированы 68 рабочих TXT.
- 18 импортированных backend-книг синхронизированы с актуальным каталогом;
  две отдельные демонстрационные книги получили явные границы абзацев.
- В общей инструкции A1-C1 закреплено правило: один абзац или отдельная
  реплика хранится одной физической строкой, новый абзац отделяется пустой
  строкой.
- Аудиты A1 и A2 теперь считают одиночный перевод строки внутри абзаца
  ошибкой; экспортёр A1 дополнительно нормализует тело книги.
- Проверки: 68/68 рабочих A1 TXT и 24/24 текстовых backend-книги без
  одиночных переносов; структурные аудиты A1 и A2 успешно; строгий
  лексический аудит A1 прошёл без hard errors.

## MT fallback для словарных карточек

- После безрезультатного каскада Wiktionary/FreeDict добавлен fallback через локальную NLLB-модель.
- MT возвращает до трёх разных вариантов: нормализуются пробелы и пунктуация, удаляются
  дубликаты, латинские ответы и варианты с соседними повторяющимися словами.
- При ошибочной лемме MT сначала переводит исходную форму слова, затем лемму.
- Используются уже созданные экземпляры NLLB для `ru` и `uk`, дополнительные модели не загружаются.
- Версии словарных source повышены до `v3`; Workbench распознаёт новые версии.
- Пересобран локальный пакет `The Wrong Classroom`. Для `downstairs|ADJ` и
  `downstair|NOUN` украинские варианты: `внизу`, `вниз`, `нижче`.
- Backend unit tests: 9 тестов, успешно.

## Dictionary POS/lemma fallback

- Найдена причина пустого русского перевода `downstairs` в мобильном пакете:
  `en_core_web_sm` размечал употребления как `downstairs|ADJ` и
  `downstair|NOUN`, тогда как Wiktionary и FreeDict содержат
  `downstairs|ADV`.
- `ContextDictionaryResolver` теперь после пустого строгого поиска повторяет
  lookup по той же лемме без POS, затем по исходной форме без POS.
- Исходный dictionary key сохраняется, поэтому мобильный reader продолжает
  находить карточку по данным `source_words`.
- Явно закрываются SQLite-соединения Wiktionary и FreeDict.
- Версии RU/UK context dictionary source повышены до `v2`; Workbench считает
  старые словари устаревшими и предлагает их пересборку.
- Добавлены regression-тесты для обоих ошибочных разборов `downstairs`.
- Пересобраны RU/UK dictionary manifest и ZIP книги `The Wrong Classroom` в
  Runtime и CloudLibrary; локальный `library_index.json` получил новый
  SHA-256.

## Удаление устаревших книг при Sync to R2

- Причиной сохранения удалённых книг в приложении был локальный `Studio/CloudLibrary`:
  построитель индекса повторно включал все накопившиеся ZIP, даже если соответствующий
  TXT уже был удалён из Workbench.
- Перед перестроением `library_index.json` синхронизация теперь сверяет публикации с
  актуальными TXT из `Studio/Books` по level, section, chapter и title.
- Для отсутствующих в Workbench книг удаляются локальные ZIP и извлечённые обложки,
  после чего новый индекс и набор файлов синхронизируются с R2.
- Добавлена защита от массового удаления: очистка прекращается, если каталог Workbench
  отсутствует или в нём не найдено ни одного допустимого TXT.
- Добавлен regression-тест удаления устаревшей публикации с сохранением актуальной.
- Из CloudLibrary и R2 удалены `New Student` и `Late for Class`; опубликованный индекс
  содержит шесть актуальных книг.

## Проверки

- Backend unit tests: 6 тестов, успешно.
- `py_compile` resolver и regression-теста: успешно.
- `dart analyze` Workbench: новых ошибок нет, остались три существующих
  предупреждения `deprecated_member_use`.
- В итоговом ZIP ключи `downstairs|ADJ` и `downstair|NOUN` содержат:
  `внизу`, `на нижнем этаже`, `вниз`, `на нижний этаж`.
- `dart analyze` изменённого построителя индекса, Workbench и тестов: без замечаний.
- Flutter tests построителя индекса и фильтров Workbench: 7/7 успешно.
- Проверен опубликованный R2 index: шесть книг, `New Student` и `Late for Class`
  отсутствуют.

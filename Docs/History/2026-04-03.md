# LEXO — история за 2026-04-03

## Сессия

Формализация `MVP9` для mobile book package, локального mobile storage и desktop-host processing model.

## Что изменено

- проанализировано текущее состояние mobile-ветки относительно iPhone/mobile runtime-модели
- зафиксировано, что текущая mobile-схема ещё зависит от desktop host через:
  - `source_path` при импорте книг
  - `audio_path` при mobile TTS playback
  - `127.0.0.1` как fallback base URL вне Android Emulator
- согласована новая продуктовая модель:
  - mobile хранит книги локально
  - desktop используется как processing-host
  - обработанная книга возвращается на mobile как переносимый package
  - desktop library может экспортироваться на mobile как локальная копия
- создан новый стратегический документ этапа:
  - `MVP/MVP9_MOBILE_BOOK_PACKAGE_PLAN.md`
- в `MVP9` зафиксировано:
  - формат `book package`
  - сценарий `Import from Mobile File`
  - сценарий `Import from Desktop Library`
  - новый API contract для mobile import/export/package download
  - отказ от desktop file paths как runtime-контракта mobile
  - переход mobile reader на локальный package как источник истины
  - новый TTS contract через local audio или explicit audio download
  - критерии готовности этапа и порядок реализации
- начата практическая реализация `MVP9`
- в backend добавлены новые mobile endpoints:
  - `POST /mobile/books/import-text`
  - `GET /mobile/desktop-books`
  - `GET /mobile/books/package`
  - `GET /mobile/books/audio`
- `engine/storage.py` расширен под:
  - импорт книги из переданного текста без `source_path`
  - сборку `book package` для mobile
  - выдачу пути к TTS audio segment для отдельного download endpoint
  - mobile `tts_manifest` snapshot внутри package
- в Flutter добавлен локальный mobile storage:
  - `app/lib/src/mobile/mobile_package_models.dart`
  - `app/lib/src/mobile/mobile_package_repository.dart`
- mobile import из `Settings` больше не отправляет на backend `source_path`
  - mobile читает `.txt` локально
  - отправляет на host текст
  - получает `book package`
  - сохраняет его в локальную mobile library
- добавлен второй mobile import-сценарий:
  - импорт готовой книги из desktop library через package download
- `MobileLibraryScreen` переведён с host-библиотеки на локальную mobile library
- `MobileReaderScreen` переведён на чтение локального package:
  - reader payload больше не загружается из desktop API
  - позиция чтения сохраняется в локальный package
  - при наличии desktop origin дополнительно пробрасывается в host для TTS `play_from_current`
- mobile TTS playback больше не открывает host `audio_path` напрямую
  - mobile скачивает нужный audio segment с host endpoint
  - сохраняет `.wav` локально
  - воспроизводит уже локальный файл
- добавлены локальные mobile app settings:
  - `app/lib/src/mobile/mobile_settings_repository.dart`
  - сохранение пользовательского `Host URL` для подключения к desktop host
- `LexoApiClient` переведён на настраиваемый runtime `baseUrl`, чтобы mobile можно было переключать на IP Windows-host для реального iPhone
- в `MobileSettingsScreen` добавлен отдельный блок host/sync:
  - просмотр текущего `Host URL`
  - ручное редактирование адреса
  - ручное обновление текущей mobile-книги из desktop library
- в mobile library добавлен ручной update flow по `desktop_book_id`
  - synced-книги помечаются отдельно
  - книгу можно обновить без повторного импорта
  - при update сохраняется локальная reading position
- подготовлен базовый LAN-режим для реальной iPhone-интеграции
- `engine/main.py` теперь поддерживает runtime bind через env:
  - `LEXO_HOST`
  - `LEXO_PORT`
- `scripts/run_lexo_mobile_android_inner.bat` теперь поднимает engine с bind на `0.0.0.0:8765`, а не только на loopback
- добавлен отдельный launcher:
  - `run_lexo_engine_lan.bat`
  - нужен для запуска только Python engine в LAN-режиме без Android Emulator
- добавлен helper script для подготовки чистой GitHub-копии проекта:
  - `make_github_repo.bat`
  - собирает рядом папку `LEXO_GITHUB` без локальных env, data, logs, build-artifacts и ephemeral-файлов
- desktop Flutter flow сохранён отдельно и не переведён на mobile package model
- backend проверен:
  - `python3 -m compileall engine` проходит
- зафиксирован следующий системный этап:
  - `MVP/MVP10_IOS_PIPELINE_AND_FIRST_INSTALL_PLAN.md`
- в `MVP10` зафиксировано:
  - GitHub repository как обязательная база этапа
  - добавление `app/ios`
  - GitHub Actions workflow для iOS build artifact
  - получение первого `.ipa` или эквивалентного iOS artifact
  - установка через `Sideloadly`
  - первая реальная установка приложения на iPhone
- проект выведен в GitHub:
  - основной remote использует GitHub repository `Vans0110/LEXO`
- в Flutter-проект добавлена iOS platform:
  - `app/ios`
- добавлен минимальный GitHub Actions workflow для первой iOS-сборки без codesign:
  - `.github/workflows/build-ios.yml`
  - workflow выполняет:
    - checkout
    - Flutter setup
    - `flutter pub get`
    - `flutter build ios --release --no-codesign`
    - упаковку `Payload/Runner.app` в unsigned `LEXO.ipa`
    - публикацию artifact `ios-ipa`
- текущий iOS CI-артефакт на этом шаге:
  - это unsigned `ipa`, а не подписанный install-ready build
  - этого достаточно для следующего шага с ручной подписью через `Sideloadly`
  - App Store / TestFlight signing pipeline ещё не настраивался
- выполнена первая реальная установка приложения на iPhone через `Sideloadly`
- на реальном iPhone выявлен сетевой блокер mobile-host связи:
  - `Host URL` был переведён с `127.0.0.1` на LAN IP Windows-host
  - engine на ПК запущен в LAN-режиме через `0.0.0.0:8765`
  - приложение на iPhone при обращении к desktop host падало с `SocketException`
- launcher-схема разделена на host и UI:
  - `run_lexo_engine_lan.bat` оставлен как основной LAN-host launcher
  - старый combined launcher перенесён в `scripts/run_lexo_mvp.bat` как legacy desktop-вариант
  - добавлен новый `run_lexo_windows_ui_lan.bat` для запуска только Windows UI без второго engine
- desktop UI получил runtime host override через `--dart-define`:
  - `app/lib/src/api/api_client.dart` теперь читает `LEXO_BASE_URL`
  - новый Windows UI launcher передаёт тот же host URL, который используется mobile-клиентом
- добавлена строгая TTS-перезапись текущего `voice + speed`:
  - в `app/lib/src/widgets/tts_panel.dart` добавлена отдельная кнопка `Overwrite` под `Generate`
  - `POST /tts/generate` теперь принимает флаг `overwrite`
  - при `overwrite=true` backend физически удаляет старые host `.wav` выбранного `voice_id + level_id`
  - после очистки создаётся новый TTS job и записывается новый звук
  - если старый job ещё в статусе `queued/generating`, strict overwrite отклоняется ошибкой
- mobile local audio cache синхронизирован с overwrite-сценарием:
  - перед strict overwrite удаляется локальный audio-cache выбранного `job_id`
  - это не даёт mobile воспроизводить устаревший скачанный сегмент после новой генерации на host
- сформулирован новый этап упрощения TTS-архитектуры:
  - создан документ `MVP/MVP11_SINGLE_BASE_TTS_AND_PLAYBACK_SPEED_PLAN.md`
  - зафиксирован переход от multi-generation по WPM к одной базовой генерации
  - зафиксирован `fixed rate = 1.0` как стартовый инженерный baseline
  - speed presets переведены в отдельный playback-layer `Slow / Normal / Fast`
  - зафиксирован отдельный слой `text normalization`
  - зафиксирован отказ от `target_wpm -> rate + pause_scale + segmentation`
- сформулирован следующий UI-этап reader playback:
  - создан документ `MVP/MVP12_BOTTOM_READER_PLAYER_PLAN.md`
  - playback controls выносятся из `TtsPanel` и mobile settings
  - в reader должен появиться нижний полупрозрачный player bar
  - generation controls и playback controls окончательно разводятся по ответственности
- начата практическая реализация `MVP12`
- создан новый reusable widget:
  - `app/lib/src/widgets/reader_playback_bar.dart`
  - нижний player bar с `expanded/collapsed` состоянием
  - центральная кнопка `Play/Pause`
  - отдельные `Stop`, `Prev`, `Next`
- desktop reader переведён на новый bottom player:
  - `app/lib/src/screens/reader_screen.dart`
  - playback controls убраны из боковой `TtsPanel`
  - внизу reader добавлен `ReaderPlaybackBar`
  - central `Play/Pause` теперь сам решает:
    - запустить ready job
    - поставить на паузу
    - продолжить
- mobile reader переведён на новый bottom player:
  - `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart`
  - нижний player bar встроен прямо в reader screen
  - playback controls убраны из mobile settings
- generation UI упрощён:
  - `app/lib/src/widgets/tts_panel.dart` теперь содержит только:
    - voice
    - speed preset
    - `Generate`
    - `Overwrite`
  - `app/lib/src/ui/mobile/screens/mobile_settings_screen.dart` больше не пробрасывает playback callbacks
- начата практическая реализация `MVP11`
- backend TTS переведён на single-base generation:
  - `engine/tts/speech_profiles.py` больше не строит `rate` из `target_wpm`
  - generation profile зафиксирован как один базовый профиль
  - `GET /tts/levels` теперь отдаёт playback presets:
    - `Slow`
    - `Normal`
    - `Fast`
  - `engine/tts/tts_service.py` больше не создаёт отдельные jobs по speed preset
  - generation job теперь фактически один на `book + voice`
  - audio cache переведён в единый базовый каталог `base`, а не в per-speed directories
- TTS segmentation отвязана от WPM:
  - `engine/tts/tts_segmenter.py` больше не выбирает chunk policy по `target_wpm`
  - оставлена одна стабильная segmentation policy
- добавлен базовый слой text normalization для TTS:
  - новый файл `engine/tts/tts_text_normalizer.py`
  - normalizer применяется к `synthesis_text`, а не к reader source text
  - нормализуются:
    - кавычки
    - тире
    - лишние пробелы и переносы
    - простые случаи `Chapter N`
    - простые случаи времени вида `7:00 AM`
- Flutter UI переведён на playback speed presets:
  - `app/lib/src/models.dart`: `TtsLevel` теперь хранит `playbackSpeed`
  - `app/lib/src/widgets/tts_panel.dart` показывает `Slow/Normal/Fast` и `x-speed`, а не WPM
  - selected generation job в UI теперь выбирается по голосу, а не по `voice + level`
- desktop и mobile player получили speed-layer поверх готового audio:
  - `app/lib/src/screens/reader_screen.dart`
  - `app/lib/src/ui/mobile/screens/mobile_reader_screen.dart`
  - на `media_kit` player теперь накладывается выбранная playback speed
  - speed preset больше не создаёт новый generation job
- после первого runtime-прослушивания базовый generation rate откалиброван:
  - `rate = 1.0` оказался слишком быстрым
  - baseline переведён на `rate = 0.89` как стартовый clean slow limit
- в iOS-конфиг добавлены разрешения для LAN-host сценария:
  - `NSAppTransportSecurity` с разрешением на `http`
  - `NSLocalNetworkUsageDescription`
  - файл: `app/ios/Runner/Info.plist`
- добавлен helper script для быстрого обновления GitHub-репозитория:
  - `push_repo_update.bat`
  - переведён в one-click режим без ручного ввода команд
  - по умолчанию добавляет только изменения уже отслеживаемых файлов через `git add -u`
  - автоматически использует commit message вида `Auto update YYYY-MM-DD_HHMMSS`
  - дополнительно подхватывает безопасные новые файлы:
    - `push_repo_update.bat`
    - `.github`
    - `scripts/make_github_repo.bat`
  - затем делает `commit` и `push origin main`
- исправлен runtime-баг в `push_repo_update.bat`:
  - была перевёрнута проверка `git diff --cached --quiet`
  - из-за этого скрипт ошибочно писал `Nothing staged for commit` даже при наличии staged-изменений
- helper script `make_github_repo.bat` перенесён из корня в:
  - `scripts/make_github_repo.bat`
- после этой правки требуется:
  - пересобрать новый `ipa`
  - переустановить приложение на iPhone
  - повторно проверить `Import from Desktop`
- выполнен детальный кодовый разбор текущей reader/translation связки:
  - подтверждено, что текущий frontend рендерит абзац через повторную regex-токенизацию `sourceText`
  - подтверждено, что `InteractiveParagraphText` сопоставляет source tokens с `words[]` только по `wordIndex`
  - подтверждено, что reader UI сейчас фактически работает как unit-level интерфейс поверх sentence-level translation и heuristic alignment
  - зафиксировано, что это даёт отдельный frontend-слой смещений поверх backend mapping-проблем
- сформулирован следующий узкий reader-этап:
  - создан документ `MVP/MVP13_STABLE_SOURCE_RENDER_PLAN.md`
  - этап ограничен устранением frontend regex-tokenization как источника рассинхрона
  - на этапе не планируется переписывать `word_alignment.py`
  - целевая модель этапа:
    - backend отдаёт стабильные source tokens для рендера
    - frontend рендерит только payload tokens
    - выбор остаётся unit-level через `tapUnitId`
    - верхний translation context пока сохраняется без изменения логики
- начата практическая реализация `MVP13`
- `engine/storage.py` расширен новым reader payload-слоем:
  - `GET /reader/paragraphs` теперь для каждого абзаца отдаёт `tokens`
  - `tokens` собираются на backend из `source_text + words`
  - backend сохраняет точный исходный текст слова, а промежутки между словами отдаёт как отдельные render-токены
  - gap-токены внутри одного `tapUnitId` получают тот же `tap_unit_id`, чтобы подсветка unit продолжала покрывать пробелы между словами
- `app/lib/src/models.dart` расширен:
  - добавлена модель `ParagraphTokenItem`
  - `ParagraphItem` теперь содержит `tokens`
  - добавлен безопасный legacy-fallback для старых payload без `tokens`, чтобы reader не становился пустым на уже сохранённых mobile packages
- `app/lib/src/widgets/interactive_paragraph_text.dart` переписан под новый render contract:
  - удалена фронтовая `_tokenize(sourceText)`
  - удалено сопоставление слов через `wordIndex`
  - рендер теперь строится только по `tokens`
  - tap по токену находит связанный `ParagraphWordItem` через `wordId`
- `app/lib/src/widgets/reader_text_flow.dart` переведён на новый paragraph render contract:
  - в renderer больше не передаётся сырой `sourceText` как источник токенизации
- backend проверен после правок:
  - `python3 -m compileall engine` проходит
- ограничения первого прохода `MVP13`:
  - Flutter runtime не проверялся из текущего окружения
  - старые mobile packages без новых `tokens` отображаются через safe fallback сплошным текстом без token-level интерактивности до обновления package
- по реальному tap-логу выявлена следующая основная backend-проблема:
  - в начале article-led noun phrase target leading function word мог занимать content-slot первого смыслового слова
  - кейс: `The Sunny Morning -> В Солнечном утром`
  - в старой логике `Sunny` ошибочно садился на `В`, а `Солнечном` уезжал в `right context`
- внесён узкий backend-фикс в `engine/word_alignment.py`:
  - исправлен `_assign_window_range`
  - для article-led windows с двумя и более source content words backend теперь может пропускать ведущий target function word при линейном распределении content-slot-ов
  - добавлен набор `TARGET_FUNCTION_WORDS` только для этой узкой стадии
  - специальные конструкции не менялись:
    - `pronoun_be`
    - `be_predicate`
    - `time`
    - `chapter`
- backend повторно проверен:
  - `python3 -m compileall engine` проходит
- по повторному tap-логу после первого фикса выявлен остаточный дефект:
  - `Sunny` уже перестал садиться на `В`
  - но article `The` продолжал наследовать target span от `Sunny`, то есть `The -> Солнечном`
- внесён второй узкий backend-фикс в `engine/word_alignment.py`:
  - исправлен `_resolve_function_target`
  - для article source-word теперь допускается отдельный leading target function-slot, если anchor-content слово уже сдвинуто вправо
  - целевой эффект для article-led phrase без unit-grouping:
    - `The -> В`
    - `Sunny/New -> Солнечном/Новый`
    - `Morning/Day -> утром/день`
- backend повторно проверен после второго фикса:
  - `python3 -m compileall engine` проходит
- по следующему разбору tap-логов подтверждено:
  - `The New Day` не собирался в существующие группы не из-за alignment, а из-за отсутствия отдельного fallback-правила `article + adjective`
  - при этом уже рабочие кейсы `The sun`, `The cat`, `a big garden` должны сохранять приоритет
- добавлен новый fallback unit-type в `engine/word_alignment.py`:
  - `article_adjective`
  - правило стоит после `article_noun_phrase`, но до `article_noun`
  - цель: собирать пары вида `The New`, когда полная `article + adjective + noun` phrase не схлопнулась по текущим boundary-правилам
  - `article_noun_phrase` и `article_noun` сохраняют более высокий смысловой приоритет
- backend повторно проверен после добавления `article_adjective`:
  - `python3 -m compileall engine` проходит
- после разбора верхней translation bar зафиксирован следующий принцип:
  - UI не должен склеивать `left + focus` наугад
  - если перевод сверху нужно показывать как одну группу, это должно приходить из backend unit-span
- внесена точечная правка в `engine/word_alignment.py`:
  - `article_adjective` теперь резолвит target-span через весь unit, а не только через adjective-word
  - целевой эффект: для кейсов вида `The Sunny` верхняя плашка должна получать подтверждённую группу вроде `В Солнечном`, а не `left="В"` + `focus="Солнечном"`
- backend повторно проверен после правки unit-span:
  - `python3 -m compileall engine` проходит

## Ограничения текущей реализации

- `flutter analyze` и `dart format` не запускались:
  - в текущем окружении отсутствуют `flutter` и `dart`
- первый проход `MVP11` не проверялся Flutter runtime из текущего окружения:
  - особенно нужен реальный smoke-test для `media_kit.setRate(...)`
- `MVP12` также не проверялся Flutter runtime из текущего окружения:
  - нужен живой smoke-test desktop/mobile на новый bottom player
- сам runtime `Host URL` уже настраиваемый, но реальная iPhone-интеграция ещё требует проверки сети:
  - IP Windows-host
  - доступность порта `8765`
  - разрешение фаервола
- фактическая проверка с real iPhone ещё не выполнялась из текущего окружения
- mobile reader уже локальный по книге, но TTS generation/control всё ещё идут через desktop host как processing service
- desktop library sync сейчас ручной:
  - import package по запросу
  - без автообновления и без diff-синхронизации
- GitHub Actions workflow пока собирает unsigned iOS artifact:
  - для реальной установки на iPhone ещё нужен шаг ручной подписи через `Sideloadly`
- текущая iPhone-проверка ещё не завершена:
  - после сетевой iOS-правки нужен повторный прогон на устройстве
- backend первого прохода `MVP11` проверен:
  - `python3 -m compileall engine` проходит

## Зачем

- текущая схема подходит для desktop и частично для Android Emulator preview, но не подходит как основа для реального iPhone runtime
- mobile-приложение не должно зависеть от файловых путей desktop-хоста
- нужен переносимый формат книги, чтобы tap units, translation context и TTS могли жить локально на устройстве
- нужен отдельный этап перед iPhone build pipeline, чтобы не тащить в iOS неправильные desktop-зависимости

## Ожидаемый результат

- следующий рабочий этап реализации идёт по утверждённой модели `desktop processing -> mobile package -> local mobile reader`
- mobile import и mobile TTS будут перепроектированы без зависимости от desktop filesystem
- появится база для локальной mobile library и синка книг из desktop-библиотеки
- после этого проект можно будет переводить к реальному iPhone pipeline без архитектурного долга по file paths

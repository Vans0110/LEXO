# 2026-07-02

## Google Play package id update

- Play Console показала, что старый package id `com.virgil.reader` уже занят; свободным выбран `com.lexo.virgil`.
- В `Virgil/App/android/app/build.gradle.kts` Android `namespace` и `applicationId` переведены на `com.lexo.virgil`; Kotlin package `MainActivity` также обновлён, файл перенесён в `com/lexo/virgil`.
- Пересобран signed Google Play AAB `1.0.0+1`; merged manifest подтверждает `package="com.lexo.virgil"`, `versionCode=1`, `versionName="1.0.0"`, `targetSdkVersion=36`.
- `Release/GooglePlay/1.0.0` обновлён под новый package id: заменён AAB `aab/Virgil-1.0.0-1.aab`, пересчитаны `manifest.json` и `SHA256SUMS.txt`.
- Новый AAB SHA-256: `d084cf685d0b1561f39c52c526d2d32bc4731414beb6be4fde2b84791b6cb0f4`; размер: `87586821` bytes.

## English Google Play listing assets

- `Studio/Tools/generate_google_play_screenshots_v2.ps1` расширен языком `en`; сгенерированы 6 английских Google Play screenshots 1080x1920 в том же дизайне, что UK/RU.
- `Studio/Tools/generate_google_play_feature_graphics.ps1` расширен английской локализацией; сгенерирован `feature_graphic.png` 1024x500.
- В `Virgil/App/assets/store/google_play_listing/en` и `Release/GooglePlay/1.0.0/store-listing/en` добавлены EN short/full description и release notes.
- `Release/GooglePlay/1.0.0` дополнен `screenshots/en`, `store-listing/en` и `internal-preview/en_preview_contact_sheet.png`; `manifest.json` и `SHA256SUMS.txt` пересчитаны.
## Mobile Settings cleanup

- В мобильных Settings удалён раздел выбора языка интерфейса; beta/about-описание оставлено и перенесено вниз над строкой версии.
## Mobile book open validation

- После скачивания мобильной книги добавлена проверка чтения локального package до показа состояния Open; ошибки Load/Open в карточке книги теперь показываются в UI вместо необработанного падения.
## Virgil version bump

- Virgil/App/pubspec.yaml подготовлен к следующему обновлению: версия поднята до 1.0.1+2, чтобы Settings показывал Version 1.0.1, а Google Play получил новый versionCode 2.
## Mobile AdMob interstitial

- В Virgil mobile подключён `google_mobile_ads` с официальными тестовыми Google AdMob ID для Android: App ID `ca-app-pub-3940256099942544~3347511713`, Interstitial Ad Unit `ca-app-pub-3940256099942544/1033173712`.
- Добавлен `MobileInterstitialAdService`: первые 10 минут после запуска без рекламы, затем interstitial не чаще одного раза в 15 минут; если реклама не готова или не загрузилась, пользовательское действие продолжается без блокировки.
- Показы подключены к Download/Update в карточке книги, переходу на вкладку Library и выходу из swipe-review карточек.
- Для выхода из swipe-review interstitial теперь вызывается только после минимум 3 действий со карточками; быстрый вход/выход из Review рекламу не показывает.
- Исправлено прокидывание `onReviewExitAd` из mobile shell в cards review; interstitial на выходе из Review теперь реально вызывается после 3 действий, а загрузка рекламы защищена флагом инициализации SDK.

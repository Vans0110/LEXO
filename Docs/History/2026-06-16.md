# 2026-06-16

## Учёт Google Translation API

- Рассчитан фактический расход Google-перевода по текущей backend-БД для двух
  тестовых книг:
  - `A Voice from Online`: 24 отправленных segment-а, 722 очищенных символа;
  - `The Wrong Classroom`: 24 отправленных segment-а, 778 очищенных символов.
- Подтверждённый локально расход по `provider_used=google-cloud-translation-v2`
  составляет 1500 символов. Если те же две книги действительно отправлялись в
  оба языка RU+UK, верхняя оценка составляет 3000 символов.
- Добавлен локальный счётчик Google Translation API:
  `Studio/Backend/data/google_translate_usage.jsonl`.
- В usage-файл внесён backfill за 2026-06-15 для RU+UK по двум книгам:
  3000 символов.
- Каждый успешный batch Google API теперь записывает дату, язык, количество
  очищенных символов и segment-ов.
- Добавлен endpoint `/google-translate/usage` с месячным итогом, лимитом
  500000 символов и safety-limit 490000.
- Workbench status panel показывает месячный расход Google chars и разбивку по
  языкам.
- Рабочий safety-limit снижен до 5000 символов в месяц: backend блокирует
  Google-запрос до HTTP-вызова, если новый batch превысит лимит.
- Workbench перед каждым текстовым Google-импортом показывает подтверждение с
  оценкой символов, текущим расходом и прогнозом после операции.
- Проверки: backend unit tests 15/15, Flutter tests 14/14, точечный Dart
  analyze изменённых файлов без замечаний.

## Workbench batch-start

- Верхняя панель Book Library переведена с отдельных кнопок обработки на один `Start`.
- Запуск теперь fail-closed: если не выбрана ни одна книга или не стоит ни одна галочка операции,
  обработка не стартует и backend не вызывается.
- `Start` использует галочки `Book translation language` (reader/text RU/UK), отдельные галочки
  `Dictionary language` (word dictionaries RU/UK) и `Kokoro voices`; каждая группа запускает только
  свой тип артефактов.
- Старые per-book action-кнопки refresh/use удалены из карточек книг; дополнительные верхние галочки
  операций не используются.
- Строка Google usage перенесена наверх Workbench; она и dialog подтверждения Google показывают рабочий cap
  `up to 5 000` отдельно от бесплатного месячного лимита `500 000`.
- Проверки: точечный Dart analyze изменённых Workbench-файлов без замечаний; `flutter test
  test/workbench_library_filters_test.dart` пройден.

## Dictionary audit tooling

- Добавлен отдельный offline-скрипт `Studio/Tools/audit_book_dictionary.py`, который строит Markdown/CSV/JSON
  таблицы для ручного просмотра словаря книги вне backend pipeline.
- Скрипт читает `dictionary_ru/uk.json`, `reader_ru/uk.json`, подтягивает source/target сегменты и оставляет
  пустые поля `review`, `suggested_override`, `notes` для ручного решения.
- Для первой книги `A Voice from Online` создан audit-вывод в
  `Studio/Runtime/dictionary_audit/book_07a16679df53/`.
- Проверка: `py_compile` нового скрипта пройден.
## Virgil Core Dictionary

- Создана папка `Studio/Backend/data/dictionaries/virgil_core/` для нового ручного словаря Virgil.
- Добавлена инструкция `README.md` с правилами построения: ключ `lowercase_word|UPPERCASE_POS`, POS обязателен,
  первый перевод главный, 3-5 переводов на язык, существующие языковые переводы не дополняются автоматически.
- Добавлена `schema.json` для проверки формы словаря.
- Создан общий стартовый `virgil_core_dictionary.json` с первыми проверенными entries из книги `A Voice from Online`.
- Создан book seed `book_seeds/book_07a16679df53_a_voice_from_online.json`, чтобы сохранить источник первых исправлений
  отдельно от общего словаря.
- `A Voice from Online` прогнана в полный book seed нового формата: 91 `word|POS` entry, RU/UK переводы из audit,
  поверх применены 13 ручных Core overrides (`a`, `ben`, `do`, `end`, `english`, `phone`, `read`, `right`, `spell`,
  `the`, `to`, `voice`, `w-a-l-k-e-r`).
- Все 5 актуальных книг A1 Chapter 1 прогнаны в единый `virgil_core_dictionary.json`: общий Core расширен до 257
  `word|POS` entries, у всех entries есть RU и UK; существующие языковые записи не перезаписывались.

## Virgil Core runtime reconnect

- Runtime-словарь переподключён на единый `Virgil Core Dictionary`: `ContextDictionaryResolver` и `DictionaryLookup`
  читают только `Studio/Backend/data/dictionaries/virgil_core/virgil_core_dictionary.json`.
- Словарные источники Wiktionary, FreeDict, 2Books lookup и NLLB/MT fallback отключены из backend lookup/manifest path.
  Если записи `word|POS` или языка нет в Core, словарь возвращает пустую entry без внешней подстановки.
- `storage.py` больше не создаёт словарные `NllbTranslator`-объекты и не строит `nllb_en_*` / `offline_en_ru`
  dictionary manifest; RU/UK манифесты идут через Core.
- Добавлен скрипт `Studio/Tools/extend_virgil_core_from_workbench_outputs.py` для дозаполнения Core из готовых
  Workbench output-книг с `cover.png` и `dictionary_ru/uk.json`.
- Core дозаполнен по 8 готовым output-книгам с обложками: обработано 16 словарных файлов и 1307 entries,
  создано 92 новых `word|POS`, добавлено 87 недостающих языковых веток; итоговый Core — 349 entries.
- Закрыты 5 оставшихся UK-пропусков вручную (`goodbye`, `grandparent`, `oh`, `out`, `up`); в Core 0 RU/UK пропусков.
- Известная плохая запись `math|NOUN` исправлена в Core: RU/UK `математика`.
- Проверки: `py_compile` изменённых backend/tool файлов, импорт `LexoStorage`, backend unittest
  `test_context_dictionary_fallback.py` — 4/4.

# 2026-07-15

## Skill словарных файлов ограничен собственными артефактами

- `$lexo-build-ru-book-layer` больше не обновляет globals, Workbench,
  CloudLibrary, application assets, packages или ZIP. Его выход ограничен
  `seed_words_ru.json`, `seed_phrases_ru.json`, `book_layer_ru.json` и
  собственным проверочным `word_to_word_ru.json`.
- Контекстные значения отделены от однословного учебного fallback:
  `translations[]` хранит только доказанные книгой значения, а
  `dictionary_translation` с источником `skill_fallback` не владеет target span.
- Добавлена occurrence-level схема: одна запись на каждый `word_id`, обязательный
  статус, причина пустого значения, владелец и общий `tap_unit_id` компонентов.
- Новый `validate_verification_word_to_word.py` сверяет последовательность всех
  source-токенов, уникальность `word_id`, статусы, target ownership, единые
  phrase-блоки и материализацию всех `source_forms[]`.
- Основной `validate_ru_book_layer.py` теперь требует sibling
  `word_to_word_ru.json` и блокирует завершение при его отсутствии или ошибках.
- `materialize_reviewed_ru_layer.py` требует проверочный word-to-word в review
  spec, валидирует его до записи и создаёт только четыре словарных файла.
- `$lexo-phrase-audit` приведён к той же границе ответственности; проектная и
  установленная копии синхронизированы.

### Проверки

- Synthetic verification tests: 4/4 успешно; проверены корректный phrase-блок,
  потерянное слово, разорванный tap unit и unresolved occurrence.
- Python compile новых и изменённых scripts — успешно.
- `git diff --check` — без ошибок whitespace.
## The Wrong Classroom пересобран новым skill

- Для `book_cad03a8f1779` пересозданы только четыре разрешённых файла:
  `seed_words_ru.json`, `seed_phrases_ru.json`, `book_layer_ru.json` и
  skill-owned `word_to_word_ru.json`; globals, Workbench, CloudLibrary и ZIP не
  изменялись.
- Из `reader_ru.json` восстановлены 24 смысловых EN/RU сегмента и все 146
  occurrence с исходными `word_id`; слой содержит 81 `lemma|POS`.
- `sit down` и `there are` материализованы едиными блоками с общими
  `owner_unit_id`/`tap_unit_id`; поглощённые компоненты имеют `empty_reason` и
  не получают перевод всей конструкции.
- Оба вхождения `and` сохранены как самостоятельные `and → и`; восстановлены
  потерянные контекстные значения `numbers → цифры`, `says → написано` и
  `wrong → той`.
- 24 occurrence без самостоятельного target span получили явно отделённый
  однословный `dictionary_translation` без target ownership; один occurrence
  классифицирован как `zero_correspondence`.
- Фраза `five minutes late` удалена: отдельные значения полностью выражают
  перевод, а изменение порядка не доказывает phrase necessity. В seed/layer и
  word-to-word осталось 5 подтверждённых фраз/блоков.
- Исправлена неполная декомпозиция `for the rest of the day`: все шесть source
  components теперь явно представлены.

### Итоговые проверки книги

- Book/seed/word-to-word validator: 24 parallel, 81 words, 146 entries,
  5 phrase blocks, `unresolved=0`, ошибок 0.
- Phrase validator: 24 parallel, 5 phrases, 24 reviewed, ошибок и предупреждений
  0.
- Synthetic occurrence tests: 4/4 успешно; Python compile успешно.

## Ручная очистка RU Globals и инкрементальный skill

- Одноразово вручную очищены `global_words_ru.json` и
  `global_phrases_ru.json`; оба файла содержат `{}`. Очистка намеренно не входит
  в новый skill.
- Создан `$lexo-add-ru-books-to-globals`, работающий только с явно выбранными
  `book_id`. Он читает книжные `seed_words_ru.json` и `seed_phrases_ru.json` и
  дополняет только два RU Global-файла.
- Word identity определена как `lemma|POS + translation`, phrase identity — как
  `normalized source + translation`. Идентичное значение пропускается с
  причиной, новый контекстный перевод создаёт variant, а новая книга или
  source form дополняет provenance существующего variant.
- `dictionary_translation` переносится только для seed-записи без контекстных
  значений и маркируется `dictionary_fallback`; skill ничего не придумывает.
- Добавлены preview без записи, атомарный `--write`, второй аудит всех вкладов
  выбранных книг, защита существующих Global translations/variants/provenance,
  проверки дублей и режим `report` со статусами `fully_applied`,
  `partially_applied`, `not_applied`, `invalid`.
- После очистки фактический report показывает 10/10 RU seed-книг как
  `not_applied`; автоматический импорт книг не выполнялся.
- Проектная и установленная копии skill синхронизированы.

### Проверки нового skill

- `quick_validate.py` — skill valid для проектной и установленной копий.
- Synthetic tests: первый add, повторный skip, новый перевод, provenance
  supplementation и not-applied report — 2/2 test methods успешно.
- Python compile — успешно; preview `The Wrong Classroom` — 0 ошибок и без
  записи в Globals; `git diff --check` — без whitespace errors.

## The Wrong Classroom добавлен в RU Globals

- `$lexo-add-ru-books-to-globals` запущен с `--write` только для
  `book_cad03a8f1779`; другие книги не обрабатывались.
- Preview: 0 ошибок, 86 первых значений и 19 дополнительных контекстных
  переводов.
- После записи `global_words_ru.json` содержит 81 уникальный `lemma|POS` и 100
  переводов; `global_phrases_ru.json` содержит 5 уникальных фраз/переводов.
- Повторный preview не добавил ничего: все 105 вкладов получили
  `skipped_existing`, ошибок 0.
- Coverage report: `The Wrong Classroom` — `fully_applied` 105/105; остальные
  9 RU seed-книг — `not_applied`; `partial=0`, `invalid=0`.

## Односторонняя пересборка Dictionary в Workbench

- `rebuild-library-dictionary` больше не читает seed-файлы, не пересобирает
  `book_layer_ru.json` и не выполняет merge книги обратно в Globals.
- Пересборка теперь только обновляет производный словарь выбранной книги из текущих
  `global_words_<lang>.json` и `global_phrases_<lang>.json`; затем Workbench создаёт
  `dictionary_<lang>.json` и новый `word_to_word_<lang>.json` из словаря и текста книги.
- В отчёт Workbench добавлены `translated/total`, число отсутствующих Global-ключей и
  число доступных Global-фраз. При полном отсутствии совпадающих Global-слов сборка
  останавливается с ошибкой.
- Контрольный запуск для `The Wrong Classroom`: 81/81 слов, missing 0, 5 фраз;
  SHA-256 обоих Globals, обоих seed-файлов и book layer не изменились.
- Проверки: backend unit tests 11/11, Flutter analyze без замечаний,
  `git diff --check` без whitespace errors в изменённых файлах.

## Standalone tap для служебных слов

- Из mobile package normalization удалено безусловное обнуление `tap_unit_id` для
  `ADP/AUX/CCONJ/DET/PART/SCONJ`. Точный word alignment теперь остаётся доступным для
  нажатия; phrase- и POS grammar-группы по-прежнему могут назначить общий tap-блок.
- Регрессионный тест прежнего запрета заменён проверкой сохранения standalone tap.
- Проверки: 6/6 mobile dictionary interaction tests, Flutter analyze без замечаний;
  debug APK установлена на `emulator-5554`.
- Live UI: `and → и` и `at → на` нажимаются отдельно; `sit down` и `there are`
  сохраняют единые phrase-блоки; `is a new` сохраняет grammar-блок.
- Удалены два оставшихся POS-запрета на detail card: служебное слово с реальным
  dictionary alignment теперь добавляется в `detailByWordId`, а long press не
  прекращается только из-за POS. Live long press подтвердил карточку `and → и`
  с полным EN/RU контекстом и действием Save.
- Источники detail sheet разделены: contextual header/target highlight остаются из
  `word_to_word`, а раздел `Words` получает значения только из книжного
  `dictionary_<lang>.json` (срез Globals) по `lemma|POS`. Добавлен регрессионный тест
  с намеренно разными dictionary/alignment значениями.
- Live-карточка `is a new` подтвердила контракт: header `новая` из alignment;
  `Words`: `is → быть`, `a → один`, `new → новая` из dictionary. Проверки 7/7,
  Flutter analyze без замечаний; debug APK установлена на эмулятор.

## Карта Virgil и архитектурная документация

- Расширен `Virgil/App/README.md`: добавлены реальные entrypoint/mode, команды,
  карта Flutter-кода, mobile package flow и краткий dictionary contract.
- Создан `Docs/VIRGIL_ARCHITECTURE.md` с картой экранов, backend/Workbench/R2,
  состава книжного пакета и владельцев Global Words, Global Phrases,
  `dictionary_<lang>.json` и `word_to_word_<lang>.json`.
- Явно зафиксирована односторонняя пересборка Globals → book dictionary →
  word-to-word и отделение skill-owned проверочных файлов от mobile runtime.
- Корневой README дополнен прямыми ссылками на быстрые и подробные карты.
- В `AGENTS.md` зафиксирован действующий GitHub-контур: обычные commit/push
  используют Git Credential Manager и проверку `git ls-remote`; `gh auth` не
  является обязательным для публикации, а mixed worktree требует явного staging.

## Единый формат словарной карточки Reader

- Из detail sheet удалён отдельный жёлтый dictionary-блок; одиночные слова и
  многословные блоки теперь используют единый вертикальный раздел `Words`.
- Каждая карточка слова в `Words` получила собственную кнопку сохранения справа.
  Окно выбора переводов и прежняя логика сохранения сохранены, но действие теперь
  относится к конкретному слову внутри выбранного phrase/grammar-блока.
- Добавлен widget-регрессионный тест вертикального порядка, отсутствия старой
  кнопки `Save` и независимого сохранения слова; `mobile_dictionary_interaction_test`
  проходит 8/8. `flutter analyze` не выявил новых ошибок, сохранились прежние
  предупреждения проекта вне изменённого контура.
- Актуальная debug APK собрана, установлена и перезапущена на `emulator-5554`.
  Live-проверка подтвердила единый `Words` для `Sara` и вертикальные карточки
  `sit`/`down` с отдельными кнопками; обе кнопки открывают выбор переводов именно
  своего слова. Жёлтый dictionary-блок отсутствует.

## Полное target-покрытие POS grammar-групп

- Общий перевод grammar-группы теперь строится по occurrence-span всех компонентов
  с непустым alignment, включая служебные POS; прежнее исключение function words
  из group span удалено без словарных слов и книжных исключений в production-коде.
- Mobile normalization сохраняет готовые `target_start_index`/`target_end_index`
  из `word_to_word` и только при их отсутствии использует текстовый fallback. Это
  устраняет выбор неправильного вхождения при повторяющихся target-словах.
- Матрица тестов расширена структурами `DET+NOUN`, `DET+ADJ+NOUN`, `AUX+ADJ`,
  `AUX+PART+VERB`, `ADP+NOUN`, `ADP+DET+NOUN` и повторяющимися target-токенами;
  mobile dictionary interaction tests проходят 9/9.
- Debug APK установлена на `emulator-5554`; live-карточка подтверждает
  `into Room → в комнату`, при этом `Words` сохраняет отдельные `into → в` и
  `room → комната / комнате / комнату`.
